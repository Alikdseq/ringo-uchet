# ✅ Команды для деплоя (актуальные)

## Текущая структура на сервере:
- **Root путь:** `/var/www/ringo-uchet`
- **Файлы уже есть** - нужен backup перед заменой

---

## 📋 Все команды для выполнения на сервере:

```bash
# 1. Создать backup текущей версии
sudo cp -r /var/www/ringo-uchet /var/www/ringo-uchet-backup-$(date +%Y%m%d-%H%M%S)

# 2. Распаковать новую версию во временную директорию
sudo unzip -o ~/web-build-latest.zip -d /tmp/web-new

# 3. Удалить старые файлы (кроме backup)
sudo rm -rf /var/www/ringo-uchet/*

# 4. Переместить новые файлы
sudo mv /tmp/web-new/* /var/www/ringo-uchet/

# 5. Установить правильные права
sudo chown -R www-data:www-data /var/www/ringo-uchet
sudo chmod -R 755 /var/www/ringo-uchet

# 6. Проверить конфигурацию Nginx
sudo nginx -t

# 7. Перезагрузить Nginx
sudo systemctl reload nginx

# 8. Очистить временные файлы
rm -rf /tmp/web-new
rm ~/web-build-latest.zip

echo "✅ Деплой завершен!"
```

---

## 🔍 Проверка после деплоя:

```bash
# Проверить что файлы на месте
ls -la /var/www/ringo-uchet/ | head -10

# Проверить размер главного файла (должен быть меньше ~4 MB)
ls -lh /var/www/ringo-uchet/main.dart.js

# Проверить статус Nginx
sudo systemctl status nginx

# Проверить логи (если нужно)
sudo tail -f /var/log/nginx/error.log
```

---

## 📊 Ожидаемый результат:

После деплоя размер `main.dart.js` должен быть **~3.98 MB** (вместо 4.16 MB сейчас), что даст **улучшение скорости загрузки**.

