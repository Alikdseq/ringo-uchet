# Быстрый старт: Оптимизированная сборка веб-версии

## 🚀 Как собрать оптимизированную веб-версию

### Windows:
```powershell
cd mobile
.\scripts\build-web-optimized.ps1
```

### Linux/Mac:
```bash
cd mobile
chmod +x scripts/build-web-optimized.sh
./scripts/build-web-optimized.sh
```

Скрипт автоматически:
- ✅ Очистит предыдущую сборку
- ✅ Соберет с флагами оптимизации (`--release --tree-shake-icons`)
- ✅ Покажет анализ размера сборки

## 📊 Ожидаемый результат

**До оптимизации:** ~9.7 МБ  
**После оптимизации:** ~5-6 МБ (без сжатия)

**С gzip на сервере:** ~2-3 МБ (реальный размер при загрузке)  
**С brotli на сервере:** ~1.5-2 МБ (реальный размер при загрузке)

## ⚙️ Что было оптимизировано

1. ✅ **Условные импорты** - sqflite исключен из веб-сборки
2. ✅ **Tree-shaking иконок** - удалены неиспользуемые Material иконки
3. ✅ **Минификация** - включена автоматически в release режиме
4. ✅ **Lazy loading** - экраны загружаются по требованию

## 🔧 Серверная оптимизация (важно!)

Для максимального уменьшения размера **обязательно включите gzip/brotli сжатие на сервере**!

Подробности: `WEB_BUILD_OPTIMIZATION.md`

