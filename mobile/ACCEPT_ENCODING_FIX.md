# 🔧 Исправление ошибки "Refused to set unsafe header Accept-Encoding"

## Проблема

В консоли браузера появлялась ошибка:
```
Refused to set unsafe header "Accept-Encoding"
```

Это происходило потому что браузер автоматически управляет заголовком `Accept-Encoding` и не позволяет JavaScript устанавливать его вручную по соображениям безопасности.

## Решение

Добавлено явное удаление заголовка `Accept-Encoding` на веб-платформе в следующих местах:

1. **`dio_client.dart`** - явно удаляем заголовок из headers на веб-платформе
2. **`auth_interceptor.dart`** - удаляем заголовок в `onRequest` и при повторных запросах после обновления токена
3. **`retry_interceptor.dart`** - удаляем заголовок при повторных попытках запросов

## Что было изменено

### dio_client.dart
- Добавлено явное удаление `Accept-Encoding` из headers на веб-платформе
- Для мобильных платформ заголовок устанавливается как и раньше

### auth_interceptor.dart
- Добавлен импорт `kIsWeb` для проверки платформы
- В `onRequest` добавлено удаление `Accept-Encoding` перед каждым запросом на веб
- При повторных запросах после обновления токена также удаляется `Accept-Encoding`

### retry_interceptor.dart
- Добавлен импорт `kIsWeb`
- Перед повторными запросами удаляется `Accept-Encoding` на веб-платформе

## Важно

Браузер **автоматически** добавляет заголовок `Accept-Encoding: gzip, deflate, br` (или другой в зависимости от поддержки) при отправке запросов, если сервер поддерживает сжатие. Поэтому нам не нужно устанавливать этот заголовок вручную на веб-платформе.

Сжатие продолжит работать корректно, так как браузер сам управляет этим заголовком.

