# 🚀 Быстрый старт - Запуск приложения

## Шаг 1: Установка Flutter

### Windows

1. **Скачайте Flutter SDK:**
   - Перейдите на https://flutter.dev/docs/get-started/install/windows
   - Скачайте последнюю стабильную версию Flutter SDK
   - Распакуйте архив (например, в `C:\src\flutter`)

2. **Добавьте Flutter в PATH:**
   - Откройте "Переменные среды" (Environment Variables)
   - В "Системные переменные" найдите `Path`
   - Добавьте путь к `flutter\bin` (например, `C:\src\flutter\bin`)
   - Перезапустите терминал

3. **Проверьте установку:**
   ```powershell
   flutter doctor
   ```

4. **Установите недостающие компоненты:**
   - Android Studio (для Android разработки)
   - Visual Studio (для Windows разработки)
   - Следуйте инструкциям `flutter doctor` для установки недостающих компонентов

## Шаг 2: Подготовка проекта

### Откройте терминал в папке проекта

```powershell
cd C:\ringo-uchet\mobile
```

### Установите зависимости

```powershell
flutter pub get
```

### Сгенерируйте код

```powershell
flutter pub run build_runner build --delete-conflicting-outputs
```

## Шаг 3: Настройка эмулятора/устройства

### Android эмулятор

1. **Откройте Android Studio**
2. **Tools → Device Manager → Create Device**
3. **Выберите устройство** (например, Pixel 5)
4. **Выберите систему** (рекомендуется API 33 или выше)
5. **Запустите эмулятор**

### Или используйте физическое устройство

1. **Включите режим разработчика** на Android устройстве
2. **Включите USB отладку**
3. **Подключите устройство к компьютеру**
4. **Разрешите отладку** на устройстве

### Проверьте подключенные устройства

```powershell
flutter devices
```

Вы должны увидеть список доступных устройств.

## Шаг 4: Запуск приложения

### Вариант 1: Запуск в dev режиме (рекомендуется для начала)

```powershell
flutter run --flavor dev -t lib/main_dev.dart
```

### Вариант 2: Запуск с выбором устройства

```powershell
flutter devices
# Выберите устройство из списка, затем:
flutter run --flavor dev -t lib/main_dev.dart -d <device-id>
```

### Вариант 3: Запуск в release режиме (для тестирования производительности)

```powershell
flutter run --release --flavor prod -t lib/main_prod.dart
```

## Шаг 5: Горячая перезагрузка

После запуска приложения:
- Нажмите `r` в терминале для горячей перезагрузки
- Нажмите `R` для полной перезагрузки
- Нажмите `q` для выхода

## 🔧 Решение проблем

### Ошибка: "Flutter command not found"
- Убедитесь, что Flutter добавлен в PATH
- Перезапустите терминал
- Проверьте: `flutter --version`

### Ошибка: "No devices found"
- Запустите эмулятор в Android Studio
- Или подключите физическое устройство
- Проверьте: `flutter devices`

### Ошибка: "Package not found"
```powershell
flutter clean
flutter pub get
flutter pub run build_runner build --delete-conflicting-outputs
```

### Ошибка: "Gradle build failed"
- Убедитесь, что Android SDK установлен
- Проверьте версию Java (нужна Java 17)
- Попробуйте: `cd android && ./gradlew clean`

### Ошибка: "Firebase not initialized"
- Для dev режима это нормально (Firebase опционален)
- Для prod/stage нужно настроить Firebase (см. FLUTTER_SETUP.md)

## 📱 Альтернативный способ: VS Code

1. **Установите расширения:**
   - Flutter
   - Dart

2. **Откройте папку `mobile` в VS Code**

3. **Нажмите F5** или выберите "Run → Start Debugging"

4. **Выберите устройство** из списка

## 🎯 Что вы увидите

После успешного запуска:
1. Приложение соберётся и установится на устройство/эмулятор
2. Откроется экран авторизации (LoginScreen)
3. Вы сможете протестировать все функции приложения

## 📝 Примечания

- **Dev flavor** использует тестовый API endpoint
- Для работы с реальным API нужно настроить backend
- Firebase настройка опциональна для dev режима
- Все данные хранятся локально в dev режиме

## 🆘 Нужна помощь?

Если возникли проблемы:
1. Проверьте `flutter doctor` - он покажет, что нужно исправить
2. Посмотрите логи в терминале
3. Проверьте, что устройство/эмулятор запущен и доступен

