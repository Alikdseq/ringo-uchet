# 🚀 Полные команды для деплоя оптимизированной сборки

## ✅ Файл для деплоя готов

- **Архив:** `mobile/build/web-optimized.zip` (1.29 MB)
- **Размер сборки:** 5.53 MB

---

## 📋 Вариант 1: Деплой через SSH (рекомендуется)

### Шаг 1: Загрузка архива на сервер

**С Windows (PowerShell):**
```powershell
# Замените user@your-server на ваши данные
scp mobile\build\web-optimized.zip user@your-server:/tmp/
```

**С Linux/Mac:**
```bash
scp mobile/build/web-optimized.zip user@your-server:/tmp/
```

---

### Шаг 2: Подключение к серверу

```bash
ssh user@your-server
```

---

### Шаг 3: Резервная копия (рекомендуется)

```bash
# Создать резервную копию текущей версии
sudo mkdir -p /var/www/ringo-uchet-backup-$(date +%Y%m%d-%H%M%S)
sudo cp -r /var/www/ringo-uchet/* /var/www/ringo-uchet-backup-$(date +%Y%m%d-%H%M%S)/ 2>/dev/null || true

# Или если директория называется иначе:
sudo mkdir -p /var/www/html-backup-$(date +%Y%m%d-%H%M%S)
sudo cp -r /var/www/html/* /var/www/html-backup-$(date +%Y%m%d-%H%M%S)/ 2>/dev/null || true
```

---

### Шаг 4: Распаковка новой версии

```bash
# Определите путь к вашей веб-директории (обычно /var/www/html или /var/www/ringo-uchet)
# Замените /var/www/html на ваш путь

# Создать директорию если не существует
sudo mkdir -p /var/www/html

# Распаковать новую версию
sudo unzip -o /tmp/web-optimized.zip -d /var/www/html/

# Установить правильные права
sudo chown -R www-data:www-data /var/www/html
sudo chmod -R 755 /var/www/html
sudo find /var/www/html -type f -exec chmod 644 {} \;
```

---

### Шаг 5: Перезагрузка Nginx

```bash
# Проверить конфигурацию
sudo nginx -t

# Если конфигурация корректна, перезагрузить
sudo systemctl reload nginx

# Или если нужно полное перезапуск
sudo systemctl restart nginx
```

---

## 📋 Вариант 2: Деплой через WinSCP (Windows GUI)

1. **Откройте WinSCP**
2. **Подключитесь к серверу** (SSH)
3. **Перейдите в директорию веб-сервера:**
   - Обычно `/var/www/html` или `/var/www/ringo-uchet`
4. **Создайте резервную копию** (скопируйте текущую директорию)
5. **Загрузите все файлы** из `mobile/build/web/` на сервер
6. **Установите права:**
   - В WinSCP: ПКМ на директории → Properties → Permissions
   - Директории: `755`
   - Файлы: `644`
   - Owner: `www-data`
   - Group: `www-data`
7. **Перезагрузите Nginx** через SSH:
   ```bash
   sudo systemctl reload nginx
   ```

---

## 📋 Вариант 3: Полный скрипт для деплоя (все в одном)

Сохраните этот скрипт на сервере как `deploy.sh`:

```bash
#!/bin/bash

# Конфигурация
WEB_DIR="/var/www/html"
BACKUP_DIR="/var/www/backups"
ZIP_FILE="/tmp/web-optimized.zip"
DATE=$(date +%Y%m%d-%H%M%S)

# Создать резервную копию
echo "📦 Создание резервной копии..."
sudo mkdir -p "$BACKUP_DIR"
sudo cp -r "$WEB_DIR" "$BACKUP_DIR/backup-$DATE" 2>/dev/null || true

# Распаковать новую версию
echo "📂 Распаковка новой версии..."
sudo mkdir -p "$WEB_DIR"
sudo unzip -o "$ZIP_FILE" -d "$WEB_DIR/"

# Установить права
echo "🔐 Установка прав..."
sudo chown -R www-data:www-data "$WEB_DIR"
sudo chmod -R 755 "$WEB_DIR"
sudo find "$WEB_DIR" -type f -exec chmod 644 {} \;

# Проверить и перезагрузить Nginx
echo "🔄 Перезагрузка Nginx..."
sudo nginx -t && sudo systemctl reload nginx

echo "✅ Деплой завершен!"
echo "📁 Резервная копия: $BACKUP_DIR/backup-$DATE"
```

Использование:
```bash
# Загрузите архив на сервер
scp mobile/build/web-optimized.zip user@server:/tmp/

# На сервере запустите скрипт
chmod +x deploy.sh
./deploy.sh
```

---

## 🔍 Проверка после деплоя

### 1. Проверить что файлы загружены

```bash
# Посмотреть содержимое директории
ls -lh /var/www/html/ | head -20

# Проверить размер
du -sh /var/www/html/
```

### 2. Проверить что сайт работает

```bash
# Проверить заголовки
curl -I https://your-site.com/

# Должен быть статус 200 OK и заголовок gzip
# Content-Encoding: gzip
```

### 3. Проверить gzip сжатие

```bash
curl -I -H "Accept-Encoding: gzip, deflate" https://your-site.com/

# Должен быть заголовок: Content-Encoding: gzip
```

### 4. Проверить Nginx конфигурацию

```bash
# Проверить статус
sudo systemctl status nginx

# Проверить логи
sudo tail -f /var/log/nginx/error.log
```

---

## 🛠️ Полезные команды для отладки

### Очистка кэша Nginx

```bash
# Очистить кэш Nginx
sudo rm -rf /var/cache/nginx/*
sudo systemctl reload nginx
```

### Просмотр логов

```bash
# Логи ошибок
sudo tail -f /var/log/nginx/error.log

# Логи доступа
sudo tail -f /var/log/nginx/access.log
```

### Восстановление из резервной копии

```bash
# Найти резервную копию
ls -lh /var/www/ringo-uchet-backup-*/

# Восстановить
sudo cp -r /var/www/ringo-uchet-backup-YYYYMMDD-HHMMSS/* /var/www/html/
sudo chown -R www-data:www-data /var/www/html
sudo systemctl reload nginx
```

---

## ⚠️ Важно

1. **Замените пути** `/var/www/html` на ваши реальные пути
2. **Убедитесь что gzip включен** в Nginx конфигурации
3. **Проверьте права доступа** - файлы должны быть доступны для www-data
4. **Сделайте резервную копию** перед деплоем
5. **Очистите кэш браузера** после деплоя (Ctrl+Shift+R)

---

## 📊 Ожидаемые результаты

После деплоя:
- ✅ **Размер сборки:** 5.53 MB (было 9.7 MB)
- ✅ **Быстрая загрузка** благодаря оптимизациям
- ✅ **Улучшенный INP** (< 200 мс)
- ✅ **Оптимизированный поиск** и списки

---

## 🔗 Связанные файлы

- Архив для деплоя: `mobile/build/web-optimized.zip`
- Конфигурация Nginx: `infra/nginx/web-optimized.conf`
- Документация: `mobile/PERFORMANCE_OPTIMIZATIONS.md`

