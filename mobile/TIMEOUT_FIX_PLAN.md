# 🔧 План исправления проблемы с таймаутами при VPN

## 🔍 Проблема

При загрузке с VPN:
- Главный экран открывается
- Данные долго грузятся с сервера
- Появляется ошибка "Превышено время ожидания"
- Данные не отображаются

## 📋 Найденные проблемы

1. **Таймаут слишком короткий** (30 секунд) - недостаточно для VPN соединений
2. **Предзагрузка блокирует** - `await Future.wait()` ждет все запросы без таймаута
3. **Нет fallback на кэш** - при ошибках таймаута возвращается пустой массив вместо использования кэша
4. **Нет отдельного таймаута для предзагрузки** - используются базовые 30 секунд

## ✅ План исправления

### 1. Увеличить базовые таймауты
- Базовый таймаут: 30 → **60 секунд** (для VPN)
- Таймаут для предзагрузки: **90 секунд**
- Таймаут для retry: увеличивается динамически

### 2. Улучшить предзагрузку данных
- Добавить общий таймаут для всех запросов предзагрузки (90 секунд)
- При таймауте использовать кэш вместо пустого массива
- Сделать предзагрузку неблокирующей - не ждать если все запросы таймаутят

### 3. Улучшить retry логику
- Увеличить задержки между retry для медленных соединений
- Добавить прогрессивное увеличение таймаутов

### 4. Улучшить обработку ошибок в сервисах
- При таймауте всегда пытаться использовать кэш
- Не показывать ошибку если есть данные в кэше

## 📝 Изменения

### Файлы для изменения:

1. **`mobile/lib/core/constants/app_constants.dart`**
   - Увеличить `defaultTimeoutSeconds`: 30 → 60
   - Добавить `preloadTimeoutSeconds`: 90

2. **`mobile/lib/app.dart`**
   - Добавить общий таймаут для `_preloadData()`
   - Улучшить fallback на кэш при ошибках

3. **`mobile/lib/core/network/interceptors/retry_interceptor.dart`**
   - Улучшить задержки для медленных соединений

4. **`mobile/lib/features/orders/services/order_service.dart`**
   - Улучшить обработку таймаутов - использовать кэш

5. **`mobile/lib/features/catalog/services/catalog_service.dart`**
   - Улучшить обработку таймаутов - использовать кэш

## 🎯 Ожидаемый результат

После исправления:
- ✅ Таймауты достаточны для VPN (60-90 секунд)
- ✅ Предзагрузка не блокирует приложение
- ✅ При таймауте используются данные из кэша
- ✅ Нет ошибок "Превышено время ожидания" - показываются кэшированные данные
- ✅ Данные обновляются в фоне когда соединение улучшится

