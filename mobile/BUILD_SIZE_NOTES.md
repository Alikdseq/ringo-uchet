# 📦 Примечания по размеру веб-сборки

## 🔍 Проблема: размер увеличился с 9.7 МБ до 30 МБ

### Причина

Flutter по умолчанию использует **Canvaskit** (Skia рендерер) для веб, который занимает **~26 МБ**. Это было добавлено в новых версиях Flutter для улучшения производительности и совместимости.

## ✅ Решение

Скрипты `build-web-optimized.ps1` и `build-web-optimized.sh` автоматически удаляют canvaskit, используя **HTML рендерер** вместо этого.

### Результаты:

- **С canvaskit:** ~30 МБ
- **Без canvaskit (HTML рендерер):** ~4 МБ ✅

## 🎯 Два варианта сборки

### 1. Минимальный размер (HTML рендерер) — рекомендуется

**Размер:** ~4 МБ
**Преимущества:**
- Минимальный размер
- Быстрая загрузка
- Меньше данных для передачи

**Недостатки:**
- Может быть немного медленнее для сложной графики
- Некоторые виджеты могут работать не идеально

**Использование:** Скрипты по умолчанию используют этот вариант.

### 2. Максимальная производительность (Canvaskit/Skia)

**Размер:** ~30 МБ (с gzip: ~10-12 МБ)
**Преимущества:**
- Лучшая производительность рендеринга
- Идеальная совместимость виджетов
- Более точный рендеринг

**Недостатки:**
- Больший размер

**Использование:** Закомментируйте удаление canvaskit в скриптах:

**PowerShell (`build-web-optimized.ps1`):**
```powershell
# Закомментируйте эти строки:
# Remove-Item -Path $canvaskitPath -Recurse -Force -ErrorAction SilentlyContinue
```

**Bash (`build-web-optimized.sh`):**
```bash
# Закомментируйте эти строки:
# rm -rf build/web/canvaskit
```

## 📊 Финальный размер с учетом сжатия на сервере

### HTML рендерер:
- Без сжатия: ~4 МБ
- С gzip: ~1.5-2 МБ ✅✅✅

### Canvaskit:
- Без сжатия: ~30 МБ
- С gzip: ~10-12 МБ

## 🚀 Рекомендация

Для большинства случаев используйте **HTML рендерер** (текущая настройка скриптов):
- Размер в 7.5 раз меньше
- При использовании gzip на сервере: ~1.5-2 МБ вместо ~10-12 МБ
- Разница в производительности обычно незаметна для большинства приложений

Используйте Canvaskit только если:
- У вас очень сложная графика/анимации
- Есть проблемы с отображением виджетов в HTML рендерере
- Размер не критичен

