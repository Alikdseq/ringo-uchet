# 📱 Настройка Android эмулятора

## Быстрый способ (через Android Studio)

### Шаг 1: Установите Android Studio

1. Скачайте Android Studio: https://developer.android.com/studio
2. Установите Android Studio
3. При первом запуске выберите "Standard" установку

### Шаг 2: Установите Android SDK

1. Откройте Android Studio
2. **Tools → SDK Manager**
3. Установите:
   - **Android SDK Platform 33** (или выше)
   - **Android SDK Build-Tools**
   - **Android Emulator**
   - **Android SDK Platform-Tools**

### Шаг 3: Создайте эмулятор

1. В Android Studio: **Tools → Device Manager**
2. Нажмите **Create Device**
3. Выберите устройство (рекомендуется **Pixel 5**)
4. Нажмите **Next**
5. Выберите систему (рекомендуется **API 33** или **API 34**)
   - Если системы нет, нажмите **Download** рядом с нужной версией
6. Нажмите **Next** → **Finish**

### Шаг 4: Запустите эмулятор

1. В Device Manager нажмите **▶ Play** рядом с созданным устройством
2. Дождитесь загрузки эмулятора (может занять несколько минут при первом запуске)

### Шаг 5: Проверьте подключение

В PowerShell выполните:
```powershell
flutter devices
```

Вы должны увидеть что-то вроде:
```
sdk gphone64 arm64 (mobile) • emulator-5554 • android-arm64 • Android 13 (API 33)
```

### Шаг 6: Запустите приложение

```powershell
cd mobile
flutter run
```

---

## Альтернативный способ: через командную строку

### Установка через командную строку

```powershell
# Установите Android SDK через chocolatey (если установлен)
choco install android-sdk

# Или скачайте SDK вручную и добавьте в PATH
```

### Создание эмулятора через командную строку

```powershell
# Список доступных системных образов
sdkmanager --list

# Установите системный образ
sdkmanager "system-images;android-33;google_apis;x86_64"

# Создайте AVD (Android Virtual Device)
avdmanager create avd -n Pixel5_API33 -k "system-images;android-33;google_apis;x86_64"

# Запустите эмулятор
emulator -avd Pixel5_API33
```

---

## Решение проблем

### Ошибка: "Android SDK not found"
- Установите Android Studio
- Добавьте Android SDK в PATH:
  - `C:\Users\<YourUser>\AppData\Local\Android\Sdk\platform-tools`
  - `C:\Users\<YourUser>\AppData\Local\Android\Sdk\emulator`

### Ошибка: "HAXM not installed" (для Intel процессоров)
- Установите Intel HAXM: https://github.com/intel/haxm/releases
- Или используйте эмулятор на основе ARM (медленнее, но работает на всех процессорах)

### Эмулятор работает медленно
- Увеличьте RAM для эмулятора (Device Manager → Edit → Show Advanced Settings)
- Используйте x86_64 образ вместо ARM
- Включите Hardware Acceleration в BIOS

### Flutter не видит эмулятор
```powershell
# Перезапустите ADB
adb kill-server
adb start-server

# Проверьте устройства
flutter devices
```

---

## Рекомендуемые настройки эмулятора

- **RAM**: 2048 MB (минимум), 4096 MB (рекомендуется)
- **VM heap**: 512 MB
- **Internal Storage**: 2048 MB
- **SD Card**: 512 MB (опционально)

---

## Быстрая проверка

После настройки выполните:
```powershell
flutter doctor
```

Все должно быть отмечено галочками ✅

