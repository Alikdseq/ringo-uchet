# 🪟 Настройка Windows для Flutter разработки

## Проблема: "Building with plugins requires symlink support"

Для работы Flutter с плагинами на Windows требуется **Developer Mode** (режим разработчика).

## Решение: Включить Developer Mode

### Способ 1: Через настройки Windows (рекомендуется)

1. **Откройте настройки разработчика:**
   ```powershell
   start ms-settings:developers
   ```
   Или вручную:
   - Нажмите `Win + I` (открыть настройки)
   - Перейдите в **Обновление и безопасность** → **Для разработчиков**
   - Или найдите "Для разработчиков" в поиске

2. **Включите режим разработчика:**
   - В разделе **Режим разработчика** переключите на **Включено**
   - Подтвердите запрос на разрешение (может потребоваться перезагрузка)

3. **Перезагрузите компьютер** (если требуется)

### Способ 2: Через реестр (для продвинутых пользователей)

```powershell
# Запустите PowerShell от имени администратора
# Выполните:
New-ItemProperty -Path "HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\AppModelUnlock" -Name AllowDevelopmentWithoutDevLicense -PropertyType DWord -Value 1
```

### Способ 3: Через групповую политику (для корпоративных систем)

1. Откройте `gpedit.msc`
2. Перейдите: **Конфигурация компьютера** → **Административные шаблоны** → **Компоненты Windows** → **Развертывание приложений**
3. Включите: **Разрешить разработку Windows Store приложений и установку их из интегрированной среды разработки**

## Проверка

После включения Developer Mode проверьте:

```powershell
cd mobile
flutter doctor
```

Должно показать, что Windows настроен правильно.

## Запуск приложения

После включения Developer Mode:

```powershell
cd mobile
flutter run -d windows
```

## Альтернатива: Запуск на Android эмуляторе

Если не хотите включать Developer Mode, можно использовать Android эмулятор:

1. Установите Android Studio
2. Создайте эмулятор (см. `SETUP_ANDROID_EMULATOR.md`)
3. Запустите:
   ```powershell
   flutter run
   ```

## Дополнительные требования для Windows

- **Visual Studio 2022** с компонентами:
  - Desktop development with C++
  - Windows 10/11 SDK
- **CMake** (обычно устанавливается с Visual Studio)

Проверьте установку:
```powershell
flutter doctor
```

## Решение других проблем

### Ошибка: "Visual Studio not found"
- Установите Visual Studio 2022 Community (бесплатно)
- При установке выберите "Desktop development with C++"

### Ошибка: "CMake not found"
- Установите CMake: https://cmake.org/download/
- Или установите через Visual Studio Installer

### Медленная сборка
- Используйте Release режим для тестирования производительности
- Или используйте Android эмулятор (быстрее для разработки)

