# 🔧 Исправление Visual Studio для Flutter Windows

## Проблема

Flutter требует следующие компоненты Visual Studio:
- MSVC v142 - VS 2019 C++ x64/x86 build tools
- C++ CMake tools for Windows
- Windows 10 SDK

## Решение: Установка недостающих компонентов

### Шаг 1: Откройте Visual Studio Installer

1. Найдите **Visual Studio Installer** в меню Пуск
2. Или выполните в PowerShell:
   ```powershell
   & "C:\Program Files (x86)\Microsoft Visual Studio\Installer\vs_installer.exe"
   ```

### Шаг 2: Измените установку Visual Studio

1. В Visual Studio Installer найдите **Visual Studio Build Tools 2022**
2. Нажмите **Изменить** (Modify)

### Шаг 3: Выберите необходимые компоненты

Установите следующие компоненты:

✅ **Desktop development with C++** (рабочая нагрузка)
   - ✅ MSVC v142 - VS 2019 C++ x64/x86 build tools (или новее)
   - ✅ C++ CMake tools for Windows
   - ✅ Windows 10 SDK (10.0.19041.0 или новее)
   - ✅ Windows 11 SDK (опционально, но рекомендуется)

### Шаг 4: Установите компоненты

1. Нажмите **Изменить** внизу окна
2. Дождитесь завершения установки (может занять 10-30 минут)
3. Перезагрузите компьютер после установки

### Шаг 5: Проверьте установку

```powershell
flutter doctor
```

Теперь должно быть:
```
[√] Visual Studio - develop Windows apps
```

---

## Альтернатива: Использование Android эмулятора

Если не хотите устанавливать Visual Studio компоненты, используйте Android эмулятор (уже настроен ✅):

```powershell
cd mobile
flutter run
```

Android эмулятор работает быстрее для разработки и не требует Visual Studio.

---

## Быстрая установка через командную строку

Если у вас установлен Visual Studio Installer, можно установить компоненты через командную строку:

```powershell
# Запустите PowerShell от имени администратора
& "C:\Program Files (x86)\Microsoft Visual Studio\Installer\vs_installer.exe" modify `
  --installPath "C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools" `
  --add Microsoft.VisualStudio.Workload.VCTools `
  --includeRecommended
```

---

## Минимальные требования

Для работы Flutter на Windows достаточно:
- **MSVC v142** или новее
- **CMake tools**
- **Windows 10 SDK** (версия 10.0.19041.0 или выше)

---

## После установки

После установки компонентов:

1. **Перезагрузите компьютер**
2. **Перезапустите терминал**
3. **Проверьте:**
   ```powershell
   flutter doctor
   ```
4. **Запустите приложение:**
   ```powershell
   cd mobile
   flutter run -d windows
   ```

