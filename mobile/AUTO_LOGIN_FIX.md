# 🔧 Исправление проблемы с автоматическим входом

## Проблема
После 1-2 часов использования приложение запрашивало пароль снова, хотя он должен был сохраняться навсегда.

## Причина
Когда токены истекали и не удавалось их обновить, код вызывал `clearAll()`, который удалял **все данные включая сохраненный пароль и телефон**.

## Исправления

### 1. Изменен метод `_refreshUserInBackground()` (строки 154-192)
**До:**
- При неудачном обновлении токена вызывался `clearAll()` → удалялся пароль

**После:**
- Используется `clearTokens()` → удаляются только токены, пароль сохраняется
- Добавлена логика автоматического входа с сохраненными данными
- Если автоматический вход не удался, но есть кэш пользователя → продолжаем работу с кэшем

### 2. Изменен метод `refreshToken()` (строки 369-387)
**До:**
- При ошибке вызывался `logout()` → мог удалить данные

**После:**
- Просто возвращает `false` без вызова `logout()`
- Вызывающий код обрабатывает ситуацию и пытается автоматический вход

## Результат

Теперь пароль и телефон **никогда не удаляются** (кроме явного выхода с очисткой):
1. ✅ При истечении токенов → пароль сохраняется
2. ✅ При неудачном обновлении токена → пароль сохраняется
3. ✅ Автоматический вход выполняется с сохраненными данными
4. ✅ Если автоматический вход не удался → работа продолжается с кэшем

## Поведение теперь

1. **Первый вход:** Пароль сохраняется
2. **Через 1-2 часа (токены истекли):**
   - Пытаемся обновить токен
   - Если не удалось → пытаемся автоматический вход с сохраненным паролем
   - Если автоматический вход успешен → пользователь остается авторизованным
   - Пароль **никогда не удаляется**

3. **При следующем открытии приложения:**
   - Если есть токен → вход автоматически
   - Если токена нет → автоматический вход с сохраненным паролем
   - Пароль остается сохраненным **навсегда**

