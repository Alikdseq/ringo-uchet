# Чек-лист перед сборкой веб-версии

## ✅ Что нужно сделать перед запуском скрипта:

### 1. Убедитесь что Flutter установлен и доступен:
```bash
flutter --version
```

### 2. Получите зависимости (уже выполнено, но можно повторить):
```bash
cd mobile
flutter pub get
```

### 3. (Опционально) Проверьте что код компилируется:
```bash
flutter analyze
```

### 4. Запустите скрипт сборки:
```powershell
# Windows
.\scripts\build-web-optimized.ps1

# Linux/Mac  
./scripts/build-web-optimized.sh
```

## ⚠️ Важно знать:

- Скрипт автоматически очистит предыдущую сборку
- Скрипт автоматически запустит `flutter pub get` перед сборкой
- После сборки будет показан анализ размера

## 🚀 Готово к запуску!

Если все вышеперечисленное выполнено, можно сразу запускать скрипт.

