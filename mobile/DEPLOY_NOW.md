# 🚀 Команды для деплоя оптимизированной сборки

## ✅ Сборка готова

- **Размер сборки:** 5.53 MB
- **Размер архива:** 1.29 MB
- **Расположение:** `mobile/build/web-optimized.zip`

---

## 📋 Команды для деплоя на сервер

### Вариант 1: Через SSH (рекомендуется)

```bash
# 1. Загрузите архив на сервер через SCP
scp mobile/build/web-optimized.zip user@your-server:/tmp/

# 2. Подключитесь к серверу
ssh user@your-server

# 3. Распакуйте архив в директорию веб-сервера
cd /var/www/html  # или ваша директория
sudo unzip -o /tmp/web-optimized.zip

# 4. Установите правильные права
sudo chown -R www-data:www-data /var/www/html
sudo chmod -R 755 /var/www/html

# 5. Перезапустите Nginx (если нужно)
sudo nginx -t
sudo systemctl reload nginx
```

### Вариант 2: Через WinSCP (Windows)

1. Откройте WinSCP
2. Подключитесь к серверу
3. Перейдите в директорию веб-сервера (например, `/var/www/html`)
4. Загрузите все файлы из `mobile/build/web/`
5. Установите права на файлы (755 для директорий, 644 для файлов)

---

## 🔍 Проверка после деплоя

1. **Откройте сайт в браузере**
2. **Проверьте консоль (F12)** - не должно быть ошибок
3. **Проверьте скорость загрузки:**
   ```bash
   curl -I -H "Accept-Encoding: gzip, deflate" https://your-site.com/
   ```
   Должен быть заголовок `Content-Encoding: gzip`

---

## 📊 Ожидаемые улучшения

- ✅ **Размер сборки:** 5.53 MB (было 9.7 MB) - **уменьшение на 43%**
- ✅ **Оптимизирован поиск** (debounce)
- ✅ **Убраны лишние перерисовки**
- ✅ **Оптимизированы списки**
- ✅ **Улучшен INP** (ожидается < 200 мс)

---

## ⚠️ Важно

- Убедитесь, что на сервере включен **gzip** сжатие в Nginx
- Проверьте, что все файлы правильно загружены
- Очистите кэш браузера после деплоя (Ctrl+Shift+R)

---

## 🔗 Полезные ссылки

- Файл сборки: `mobile/build/web-optimized.zip`
- Конфигурация Nginx: `infra/nginx/web-optimized.conf`
- Документация по оптимизации: `mobile/PERFORMANCE_OPTIMIZATIONS.md`

