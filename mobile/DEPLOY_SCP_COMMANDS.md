# 📤 Правильные команды для загрузки через SCP

## ✅ Файл готов

**Расположение:** `C:\ringo-uchet\mobile\build\web-optimized.zip` (1.29 MB)

---

## 🎯 Правильная команда (в зависимости от вашей директории)

### Вариант 1: Вы находитесь в `mobile\build` (как сейчас)

```powershell
# Просто имя файла, так как вы уже в нужной директории
scp web-optimized.zip root@91.229.90.72:~/web-build-latest.zip
```

### Вариант 2: Вы находитесь в `mobile`

```powershell
scp build\web-optimized.zip root@91.229.90.72:~/web-build-latest.zip
```

### Вариант 3: Вы находитесь в корне проекта `C:\ringo-uchet`

```powershell
scp mobile\build\web-optimized.zip root@91.229.90.72:~/web-build-latest.zip
```

### Вариант 4: Абсолютный путь (работает из любой директории)

```powershell
scp C:\ringo-uchet\mobile\build\web-optimized.zip root@91.229.90.72:~/web-build-latest.zip
```

---

## ❌ Почему была ошибка?

Вы находились в `mobile\build` и использовали путь `mobile/build/web-optimized.zip`, который ищет файл в `mobile\build\mobile\build\web-optimized.zip` - поэтому "No such file or directory".

**Решение:** Когда вы уже в нужной директории, используйте просто имя файла!

---

## ✅ Рекомендуемая команда (откуда угодно)

```powershell
# Из любой директории используйте абсолютный путь
scp C:\ringo-uchet\mobile\build\web-optimized.zip root@91.229.90.72:~/web-build-latest.zip
```

---

## 🚀 После загрузки на сервер

```bash
# Подключитесь к серверу
ssh root@91.229.90.72

# Распакуйте
sudo unzip -o ~/web-build-latest.zip -d /var/www/html/

# Установите права
sudo chown -R www-data:www-data /var/www/html
sudo chmod -R 755 /var/www/html

# Перезагрузите Nginx
sudo nginx -t && sudo systemctl reload nginx
```

