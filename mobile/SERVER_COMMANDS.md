# 🖥️ Команды для выполнения на сервере

## ✅ Файл загружен на сервер

Файл должен быть в домашней директории: `~/web-build-latest.zip`

---

## 📋 Все команды для выполнения на сервере

### Шаг 1: Резервная копия (рекомендуется)

```bash
# Создать резервную копию текущей версии
sudo mkdir -p /var/www/backup-$(date +%Y%m%d-%H%M%S)
sudo cp -r /var/www/html/* /var/www/backup-$(date +%Y%m%d-%H%M%S)/ 2>/dev/null || true

# Или если у вас другой путь (например /var/www/ringo-uchet):
# sudo cp -r /var/www/ringo-uchet/* /var/www/backup-$(date +%Y%m%d-%H%M%S)/ 2>/dev/null || true
```

---

### Шаг 2: Распаковка новой версии

```bash
# Создать директорию если не существует
sudo mkdir -p /var/www/html

# Распаковать архив
sudo unzip -o ~/web-build-latest.zip -d /var/www/html/

# Или если у вас другой путь:
# sudo unzip -o ~/web-build-latest.zip -d /var/www/ringo-uchet/
```

---

### Шаг 3: Установка прав доступа

```bash
# Установить владельца
sudo chown -R www-data:www-data /var/www/html

# Установить права на директории (755)
sudo chmod -R 755 /var/www/html

# Установить права на файлы (644)
sudo find /var/www/html -type f -exec chmod 644 {} \;

# Или если у вас другой путь:
# sudo chown -R www-data:www-data /var/www/ringo-uchet
# sudo chmod -R 755 /var/www/ringo-uchet
# sudo find /var/www/ringo-uchet -type f -exec chmod 644 {} \;
```

---

### Шаг 4: Проверка и перезагрузка Nginx

```bash
# Проверить конфигурацию Nginx
sudo nginx -t

# Если проверка прошла успешно, перезагрузить Nginx
sudo systemctl reload nginx

# Или если нужно полное перезапуск
# sudo systemctl restart nginx
```

---

## ✅ Полный скрипт (все команды подряд)

Скопируйте и выполните все эти команды:

```bash
# 1. Резервная копия
sudo mkdir -p /var/www/backup-$(date +%Y%m%d-%H%M%S)
sudo cp -r /var/www/html/* /var/www/backup-$(date +%Y%m%d-%H%M%S)/ 2>/dev/null || true

# 2. Распаковка
sudo mkdir -p /var/www/html
sudo unzip -o ~/web-build-latest.zip -d /var/www/html/

# 3. Права
sudo chown -R www-data:www-data /var/www/html
sudo chmod -R 755 /var/www/html
sudo find /var/www/html -type f -exec chmod 644 {} \;

# 4. Перезагрузка Nginx
sudo nginx -t && sudo systemctl reload nginx

echo "✅ Деплой завершен!"
```

---

## 🔍 Проверка после деплоя

### Проверить что файлы распаковались

```bash
# Посмотреть содержимое
ls -lh /var/www/html/ | head -20

# Проверить размер
du -sh /var/www/html/
```

### Проверить что сайт работает

```bash
# Проверить заголовки
curl -I https://your-site.com/

# Должен быть статус 200 OK
```

### Проверить gzip сжатие

```bash
curl -I -H "Accept-Encoding: gzip, deflate" https://your-site.com/

# Должен быть заголовок: Content-Encoding: gzip
```

### Проверить статус Nginx

```bash
# Статус службы
sudo systemctl status nginx

# Посмотреть логи ошибок
sudo tail -20 /var/log/nginx/error.log

# Посмотреть логи доступа
sudo tail -20 /var/log/nginx/access.log
```

---

## 🔄 Восстановление из резервной копии (если нужно)

```bash
# Найти резервную копию
ls -lh /var/www/backup-*/

# Восстановить (замените YYYYMMDD-HHMMSS на реальную дату)
sudo cp -r /var/www/backup-YYYYMMDD-HHMMSS/* /var/www/html/
sudo chown -R www-data:www-data /var/www/html
sudo systemctl reload nginx
```

---

## ⚠️ Важно

1. **Замените `/var/www/html`** на ваш реальный путь к веб-директории, если он отличается
2. **Убедитесь что gzip включен** в конфигурации Nginx
3. **Очистите кэш браузера** после деплоя (Ctrl+Shift+R)

---

## 🎯 Быстрая версия (без резервной копии)

Если хотите быстро задеплоить без резервной копии:

```bash
sudo unzip -o ~/web-build-latest.zip -d /var/www/html/
sudo chown -R www-data:www-data /var/www/html
sudo chmod -R 755 /var/www/html
sudo nginx -t && sudo systemctl reload nginx
```

---

## 📊 Ожидаемый результат

После выполнения всех команд:
- ✅ Новая версия установлена
- ✅ Права настроены
- ✅ Nginx перезагружен
- ✅ Сайт должен работать быстрее (5.53 MB вместо 9.7 MB)

