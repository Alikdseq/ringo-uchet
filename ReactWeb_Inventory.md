## 📋 Инвентаризация текущего фронта (Flutter) и backend API для React Web

Документ реализует шаг **W0.1** из `ReactWebFrontendPlan.md`: фиксирует все экраны текущего Flutter‑приложения и **используемые** backend‑эндпоинты с их назначением, методами и основными полями запросов/ответов.  
Используется как база для проектирования и разработки React/Next.js фронта.

---

## 1. Экраны и фичи текущего Flutter‑приложения

### 1.1 Auth
- **LoginScreen**
  - Вход по телефону или email + пароль.
  - Валидация формата телефона/email.
  - Обработка ошибок (неверный логин/пароль, rate limit).
  - Автоматический редирект на `/home` при успешной авторизации.
- **RegisterScreen**
  - Регистрация нового пользователя (оператора): имя, фамилия, телефон, пароль.
  - После успешной регистрации — автоматический логин и обновление профиля.
- **AuthNotifier / AuthState (логика)**
  - Хранение `access`/`refresh` токенов и `UserInfo`.
  - Авто‑логин:
    - Восстановление пользователя из кэша.
    - Обновление токенов при старте (refresh).
    - При истечении refresh — попытка логина по сохранённым кредам (phone/email+password).
  - Смена пароля текущего пользователя.

### 1.2 Главный экран и навигация
- **RingoApp / _HomeScreen**
  - Глобальная навигация по ролям:
    - `admin`: Dashboard, Заявки, Номенклатура, Отчёты, Профиль.
    - `operator`: Dashboard, Заявки (только свои), Зарплаты, Профиль.
    - другие роли: упрощённые наборы.
  - Адаптивная навигация:
    - Desktop: AppBar + Drawer + нижняя NavigationBar.
    - Mobile: нижняя панель навигации (Orders, Catalog, Reports/Salary, Profile).
  - Splash‑экран с анимацией и проверкой статуса auth.

### 1.3 Dashboard (DashboardScreen)
- KPI‑карточки:
  - Новые заявки (CREATED/APPROVED).
  - Назначенные заявки (IN_PROGRESS).
  - Доход (для admin — сумма `total_amount` завершённых заявок).
- Мини‑график:
  - Количество заявок по дням за последние 7 дней.
- Быстрые действия:
  - Создание заявки (wizard CreateOrderScreen).
  - Переход к отчётам (для admin).
- Использует:
  - Кэш заявок (CacheService).
  - OrderService.getOrders(useCache: true).

### 1.4 Список заявок (OrdersListScreen)
- Табы:
  - «Все» + по статусам: CREATED, APPROVED, IN_PROGRESS, COMPLETED, CANCELLED.
- Поиск:
  - По номеру заявки, клиенту, адресу (debounce).
- Фильтрация по роли:
  - Для оператора — только заявки, где он назначен (по `operatorId` или `operator.id`).
- Отображение:
  - Карточки заявок с номером, клиентом, адресом, периодом, суммой (кроме оператора), статусом.
  - Оффлайн‑баннер (нет интернета).
  - Индикатор оффлайн‑очереди.
- Источники данных:
  - `OrderService.getOrders` (с кэшем и fallback при сетевых ошибках).

### 1.5 Деталь заявки (OrderDetailScreen)
- Общая информация:
  - Номер, статус, даты создания/начала/окончания.
  - Клиент, телефон, email, адрес, геокоординаты.
  - Менеджер, операторы (один/несколько), создатель.
- Позиции заказа:
  - Список `OrderItem` (equipment/service/material):
    - количество, единицы, цена, скидка, налоги, итог по строке.
  - Поддержка дополнительных расходов: топливо, ремонт техники.
- Финансовый блок:
  - `prepayment_amount`, `prepayment_status`, `total_amount`, `price_snapshot`.
- Таймлайн статусов:
  - Список `OrderStatusLog` — статус до/после, автор, комментарий, время.
- Фото:
  - Список `PhotoEvidence` (тип, ссылка, gps, время).
- Действия:
  - Обновление основных полей (адрес, описание, стоимость).
  - Смена статуса (с диалогом комментария и финансовых параметров).
  - Переход к экрану завершения (CompleteOrderScreen).
  - Генерация/скачивание PDF (чек/акт).

### 1.6 Создание/редактирование заявки (CreateOrderScreen / OrderEditScreen)
- Мастер создания:
  - Данные клиента (имя, телефон, адрес).
  - Данные заявки: адрес, описание, даты/время.
  - Выбор операторов (один и/или несколько).
  - Выбор номенклатуры (через диалог выбора Equipment/Services/Materials/Attachments).
  - Примерная стоимость (total amount).
  - Возможность сохранить **черновик** (DRAFT):
    - Автосохранение черновика (для admin) при выходе.
    - Загрузка последнего черновика при входе в экран.
- Редактирование:
  - Редактирование существующей заявки (OrderEditScreen) — изменение позиций и операторов.

### 1.7 Завершение заявки (CompleteOrderScreen)
- Ввод фактических параметров:
  - Для техники: смены, часы, дневная ставка, почасовая ставка.
  - Материалы/услуги: количество, скидка.
  - Дополнительные расходы: топливо, ремонт.
- Пересчёт итога и формирование структуры для backend `/orders/{id}/complete/`.
- Опциональная зарплата операторам (для совместимости).
- Финальное изменение статуса и генерация PDF/чека.

### 1.8 Каталог (CatalogScreen и NomenclatureManagement)
- Вкладки:
  - Техника.
  - Услуги.
  - Грунт/материалы.
  - Инструмент.
  - Навеска.
- Поиск и фильтры:
  - По имени, коду, категориям, статусу.
- Управление (для admin):
  - Создание/редактирование/удаление элементов номенклатуры.
  - Отдельный экран управления номенклатурой с кэшем (NomenclatureManagementScreen).

### 1.9 Отчёты (ReportsScreen)
- Вкладки:
  - Общие (summary): выручка, расходы, маржа, показатели по периодам.
  - По технике (equipment): загрузка, расходы, топливо, ремонт.
  - По сотрудникам (employees): выручка/часы/кол-во назначений по операторам.
- Фильтр по периоду (DateRangePicker).
- Обновление отчётов при изменении статусов заявок (через `reportsRefreshProvider`).

### 1.10 Профиль и зарплаты
- **ProfileScreen**
  - Отображение текущего пользователя (ФИО, роль, контакты).
  - Переход к:
    - Настройкам уведомлений.
    - Смене пароля.
    - Экрану оффлайн‑очереди.
  - Выход (logout) с очисткой токенов и редиректом на login.
- **ChangePasswordScreen**
  - Форма смены пароля: старый, новый, подтверждение.
- **OperatorSalaryScreen**
  - История начислений оператору:
    - Список заказов с суммой зарплаты по каждому.
    - Итоговая сумма за выбранный период.

### 1.11 Уведомления
- **NotificationSettingsScreen**
  - Управление `NotificationPreferences`:
    - включение/отключение типов уведомлений:
      - создание заявки;
      - изменение статуса;
      - получение платежа;
      - готовность счёта;
      - заполнение кассы.
  - Синхронизация настроек с сервером.

### 1.12 Оффлайн‑функции
- **OfflineBanner**
  - Индикация режима «нет интернета».
- **OfflineQueueScreen**
  - Просмотр и управление очередью оффлайн‑действий:
    - создание/обновление заявок;
    - смена статусов;
    - загрузка фото.
  - Возможность ручного ретрая и удаления элементов очереди.
- **OfflineQueueIndicator**
  - Чип с количеством элементов в очереди в верхней части экрана.

---

## 2. Backend API: инвентаризация используемых эндпоинтов

Ниже перечислены **фактически используемые** фронтом эндпоинты, сгруппированные по модулям.  
Описание основано на сервисах Flutter: `auth_service.dart`, `order_service.dart`, `catalog_service.dart`, `user_service.dart`, `finance_service.dart`, `notification_service.dart`, `client_service.dart`.

### 2.1 Auth

#### `POST /token/`
- **Назначение**: логин по телефону/email/username и паролю.
- **Запрос** (одно из полей идентификатора + пароль):
  - `phone?: string`
  - `email?: string`
  - `username?: string`
  - `password: string`
  - `captcha_token?: string`
- **Ответ** (`AuthResponse`):
  - `access: string` — JWT access‑токен.
  - `refresh: string` — JWT refresh‑токен.
  - `user?: UserInfo` — информация о пользователе (если backend сразу её отдаёт).
- **Роли**: все (анонимный вход).
- **Где используется**: `LoginScreen`, автологин после `RegisterScreen`, `AuthNotifier.login`.

#### `POST /auth/otp/send/`
- **Назначение**: отправка OTP‑кода на телефон.
- **Запрос**:
  - `phone: string`
  - `captcha_token?: string`
- **Ответ**: 200 OK или ошибка валидации / rate limit.
- **Роли**: аноним.
- **Используется**: потенциальный вход по OTP (в текущем UI может быть скрыт/не активен).

#### `POST /auth/otp/verify/`
- **Назначение**: верификация OTP‑кода и выдача токенов.
- **Запрос**:
  - `phone: string`
  - `code: string`
- **Ответ**: `AuthResponse` (аналогично `/token/`).
- **Роли**: аноним.

#### `POST /token/refresh/`
- **Назначение**: обновление access‑токена по refresh.
- **Запрос**:
  - `refresh: string`
- **Ответ**:
  - `access: string`
- **Особенности**:
  - 401 означает истёкший/невалидный refresh.
- **Используется**: `AuthNotifier.refreshToken`, авто‑логин при старте.

#### `POST /token/blacklist/`
- **Назначение**: logout — отзыв refresh‑токена.
- **Запрос**:
  - `refresh: string`
- **Ответ**: 200/204; ошибки игнорируются на фронте.
- **Используется**: logout в профиле/аппбаре.

#### `GET /users/me/`
- **Назначение**: получение текущего пользователя по access‑токену.
- **Ответ**: `UserInfo`.
- **Роли**: авторизован любой.
- **Используется**: инициализация сессии, `AuthNotifier._refreshUserInBackground`.

#### `POST /users/register/`
- **Назначение**: регистрация нового пользователя (оператора).
- **Запрос**:
  - `phone: string`
  - `password: string`
  - `first_name: string`
  - `last_name: string`
- **Ответ**: 201/200 c данными пользователя или без (фронт не зависит от структуры).
- **Используется**: `RegisterScreen`.

#### `POST /users/change-password/`
- **Назначение**: смена пароля текущего пользователя.
- **Запрос**:
  - `old_password: string`
  - `new_password: string`
  - `confirm_password: string`
- **Ответ**: 200 OK или ошибки валидации/авторизации.
- **Используется**: `ChangePasswordScreen`.

---

### 2.2 Users

#### `GET /users/operators/` (и fallback‑варианты `/api/v1/users/operators/`, `/api/users/operators/`)
- **Назначение**: получение списка пользователей с ролью `operator`.
- **Ответ**: список `UserInfo` (или paginated в поле `results`).
- **Роли**: admin/manager (для назначения операторов).
- **Используется**:
  - Выбор операторов в создании/редактировании заявки (`CreateOrderScreen`, `OrderEditScreen`).
  - Фильтры/отчёты по операторам.

#### `GET /users/` (и `/api/users/`, `/api/v1/users/` с фильтром `role=operator`)
- **Назначение**: fallback, если нет отдельного endpoint для операторов.
- **Используется**: внутри `UserService._getOperatorsFromApi`.

#### `GET /users/operator/salary/`
- **Назначение**: сводка по зарплате текущего оператора.
- **Ответ** (примерно):
  - `total_salary: string|number`
  - `salary_records: [...]`
  - `orders: [...]` — заказы с полем суммы по оператору.
- **Роли**: `operator`.
- **Используется**: `OperatorSalaryScreen`.

---

### 2.3 Orders

#### `GET /orders/`
- **Назначение**: список заявок.
- **Параметры query**:
  - `status?: string` (например, `CREATED`, `IN_PROGRESS`).
  - `search?: string` (по номеру, клиенту, адресу).
  - `page?: number`, `page_size?: number` (пагинация, по умолчанию 100).
- **Ответ**:
  - Либо список `Order[]`, либо объект с полем `results: Order[]`.
- **Роли**:
  - `admin/manager`: все заявки.
  - `operator`: только назначенные (часть фильтра реализуется на фронте по `operatorId/operator`).
- **Используется**: Dashboard, OrdersListScreen, выбор заказов для отчётов.

#### `GET /orders/{id}/`
- **Назначение**: детальная информация о заявке.
- **Ответ**: `Order` с вложенными `OrderItem[]`, `OrderStatusLog[]`, `PhotoEvidence[]`.
- **Используется**: OrderDetailScreen, OrderEditScreen, CompleteOrderScreen.

#### `POST /orders/`
- **Назначение**: создание заявки (в том числе черновика).
- **Тело (OrderRequest)**:
  - `number?: string` (может быть пустой — backend сам генерирует).
  - `client_id?: number`
  - `address: string`
  - `geo_lat?: number`, `geo_lng?: number`
  - `start_dt: string (ISO, UTC, без миллисекунд, с Z)`
  - `end_dt?: string (ISO)`
  - `description: string`
  - `status?: OrderStatus` (для черновиков — `DRAFT`)
  - `manager_id?: number`
  - `operator_id?: number` (совместимость)
  - `operator_ids?: number[]`
  - `prepayment_amount?: number`
  - `total_amount?: number`
  - `items?: OrderItem[]` (создаёт/обновляет позиции).
- **Ответ**: созданный `Order`.
- **Используется**: CreateOrderScreen, автосохранение черновика.

#### `PATCH /orders/{id}/`
- **Назначение**: обновление заявки.
- **Тело**: частичный `OrderRequest` (см. выше, с правилами по `items`).
- **Ответ**: обновлённый `Order`.
- **Используется**: OrderEditScreen, обновления из OrderDetailScreen.

#### `PATCH /orders/{id}/status/`
- **Назначение**: смена статуса заявки.
- **Тело (`OrderStatusRequest`)**:
  - `status: OrderStatus` (DRAFT/CREATED/APPROVED/IN_PROGRESS/COMPLETED/CANCELLED).
  - `comment?: string`
  - `attachment_url?: string`
  - `operator_salary?: number`
  - `fuel_expense?: number`
- **Ответ**: статус/логи или ничего; фронт затем запрашивает `GET /orders/{id}/`.
- **Используется**: ChangeStatusDialog, OrderDetailScreen.

#### `POST /orders/{id}/calculate/preview/`
- **Назначение**: предпросмотр расчёта цены по списку позиций.
- **Тело (`OrderPricePreviewRequest`)**:
  - `items: OrderItem[]`
- **Ответ (`OrderPricePreviewResponse`)**:
  - `items: Map<string, any>[]`
  - `total: number`
  - `details?: Map<string, any>`
- **Используется**: мастер создания/редактирования для показа предварительной стоимости.

#### `POST /orders/{id}/complete/`
- **Назначение**: завершение заявки с фиксацией фактических объёмов работ и затрат.
- **Тело**: структура с выбранными элементами номенклатуры, сменами/часами, расходами (см. реализацию во Flutter `CompleteOrderScreen`).
- **Ответ**: обновлённый `Order` (статус COMPLETED).
- **Используется**: CompleteOrderScreen.

#### `POST /orders/{id}/attachments/`
- **Назначение**: получение presigned URL для загрузки файла (фото и др.).
- **Тело**:
  - `file_name: string`
  - `content_type: string`
- **Ответ**:
  - Данные для загрузки файла в S3/MinIO (URL + поля формы).
- **Используется**: загрузка фото‑доказательств (функционал частично реализован/планируется).

#### `POST /orders/{id}/generate_invoice/`
- **Назначение**: инициировать генерацию PDF счёта/акта.
- **Ответ**: объект с данными инвойса/статусом задачи (точная структура зависит от backend).
- **Используется**: OrderDetailScreen, финансовый блок.

#### `GET /orders/{id}/receipt/`
- **Назначение**: получение PDF‑чека/акта в виде байтов.
- **Ответ**: `bytes` (PDF), тип ответа `application/pdf`.
- **Используется**: скачивание/просмотр чека после завершения заявки.

#### `POST /orders/{id}/delete/`
- **Назначение**: полное удаление заявки из базы (или пометка как удалённой на backend).
- **Ответ**: 200/204.
- **Используется**: административные операции из OrderDetailScreen/OrderEditScreen.

---

### 2.4 Catalog

#### `GET /equipment/`
- **Назначение**: список техники.
- **Параметры**:
  - `status?: string` (available/busy/maintenance/inactive).
  - `search?: string`
  - `page_size?: number` (по умолчанию 100).
- **Ответ**: список или пагинированный список `Equipment`.
- **Используется**: CatalogScreen (вкладка «Техника»), выбор техники в заявках, отчёты.

#### `GET /services/`
- **Назначение**: список услуг.
- **Параметры**:
  - `category?: number`
  - `search?: string`
  - `page_size?: number`
- **Ответ**: список `ServiceItem`.
- **Используется**: CatalogScreen (вкладка «Услуги»), выбор услуг в заявках.

#### `GET /materials/`
- **Назначение**: список материалов (грунт и др.).
- **Параметры**:
  - `search?: string`
  - `category?: string`
  - `page_size?: number`
- **Ответ**: список `MaterialItem`.
- **Используется**: CatalogScreen (вкладка «Материалы»), выбор материалов в заявках.

#### `GET /attachments/`
- **Назначение**: список навесок оборудования.
- **Параметры**:
  - `search?: string`
- **Ответ**: список `Attachment`.
- **Используется**: выбор навесок при конфигурации техники и расчётах.

#### `GET /clients/`
- **Назначение**: список клиентов.
- **Параметры**:
  - `search?: string`
- **Ответ**: список или пагинированный список `Client` (как `Map<String,dynamic>`).
- **Используется**: выбор/поиск клиента при создании заявок, управление клиентами.

#### `POST /clients/`
- **Назначение**: создание клиента.
- **Тело**:
  - `name: string`
  - `phone: string`
  - `address?: string`
  - `email?: string`
  - `contact_person?: string`
- **Ответ**: созданный клиент (`id`, остальные поля).
- **Используется**: CreateOrderScreen (создание клиента «на лету», в том числе для черновиков).

> CRUD для техники/услуг/материалов/навесок/клиентов (админские операции) также доступны, но их точные эндпоинты и тела могут зависеть от настроек Django ViewSet’ов; они будут уточняться на этапе реализации админской части React.

---

### 2.5 Reports / Finance

#### `GET /reports/summary/`
- **Назначение**: общий финансовый отчёт.
- **Параметры**:
  - `from?: YYYY-MM-DD`
  - `to?: YYYY-MM-DD`
- **Ответ (`SummaryReport`)**:
  - `revenue`, `revenue_from_services`, `revenue_from_equipment`
  - `expenses`, `expenses_fuel`, `expenses_repair`
  - `salaries`, `margin`, `orders_count`
  - `period?: { from?: string, to?: string }`
- **Используется**: ReportsScreen (вкладка «Общие»), Dashboard KPI.

#### `GET /reports/equipment/`
- **Назначение**: отчёт по технике.
- **Параметры**:
  - `from?: YYYY-MM-DD`
  - `to?: YYYY-MM-DD`
- **Ответ**: массив `EquipmentReportItem`:
  - `equipment_id`, `equipment_name`, `code`, `status`
  - `total_hours`, `revenue`, `expenses`, `fuel_expenses`, `repair_expenses`.

#### `GET /reports/employees/`
- **Назначение**: отчёт по сотрудникам/операторам.
- **Параметры**:
  - `from?: YYYY-MM-DD`
  - `to?: YYYY-MM-DD`
- **Ответ**: массив `EmployeeReportItem`:
  - `user_id`, `full_name`, `total_amount`, `total_hours`, `assignments`.

---

### 2.6 Notifications

#### `POST /notifications/device-tokens/`
- **Назначение**: регистрация FCM‑токена устройства.
- **Тело**:
  - `token: string`
  - `platform: 'android' | 'ios' | 'web'`
  - `app_version: string`
  - `device_info: { ... }` (зависит от платформы: модель, версия ОС и т.п.).
- **Используется**: инициализация уведомлений при логине/старте приложения.

#### `GET /notifications/preferences/preferences/`
- **Назначение**: получение настроек уведомлений текущего пользователя.
- **Ответ**: объект `NotificationPreferences`:
  - `order_created`, `status_changed`, `payment_received`, `invoice_ready`, `kassa_full`.

#### `POST /notifications/preferences/preferences/`
- **Назначение**: обновление настроек уведомлений.
- **Тело**: `NotificationPreferences.toJson()`.
- **Ответ**: обновлённые настройки.

#### `GET /notifications/preferences/logs/`
- **Назначение**: история уведомлений (лог событий).
- **Ответ**: список объектов с основными полями (тип, дата, сообщение и т.п.).
- **Используется**: потенциальный экран истории уведомлений (пока не реализован во Flutter, но API есть).

---

## 3. Связка экранов и эндпоинтов (кратко)

| Экран Flutter                             | Основные эндпоинты                                   |
|-------------------------------------------|------------------------------------------------------|
| LoginScreen, RegisterScreen               | `/token/`, `/users/register/`, `/token/refresh/`, `/users/me/` |
| DashboardScreen                           | `/orders/` (list), `/reports/summary/` (косвенно)   |
| OrdersListScreen                          | `/orders/`                                           |
| OrderDetailScreen                         | `/orders/{id}/`, `/orders/{id}/status/`, `/orders/{id}/complete/`, `/orders/{id}/generate_invoice/`, `/orders/{id}/receipt/` |
| CreateOrderScreen, OrderEditScreen        | `/orders/`, `/orders/{id}/`, `/orders/{id}/calculate/preview/`, `/clients/`, `/users/operators/` |
| CompleteOrderScreen                       | `/orders/{id}/complete/`, `/orders/{id}/status/`    |
| CatalogScreen                             | `/equipment/`, `/services/`, `/materials/`, `/attachments/`, `/clients/` |
| NomenclatureManagementScreen (admin)      | те же + CRUD эндпоинты ViewSet’ов                   |
| ReportsScreen (summary, equipment, employees) | `/reports/summary/`, `/reports/equipment/`, `/reports/employees/` |
| ProfileScreen                             | `/users/me/`, `/token/blacklist/`                   |
| ChangePasswordScreen                      | `/users/change-password/`                           |
| OperatorSalaryScreen                      | `/users/operator/salary/`                           |
| NotificationSettingsScreen                | `/notifications/preferences/preferences/`, `/notifications/preferences/logs/` |
| Оффлайн‑очередь, sync‑сервисы             | косвенно: те же эндпоинты через очередь (`/orders/...`) |

Этот документ можно расширять по мере уточнения полных схем ответов Swagger/OpenAPI, но он уже достаточно детализирован для начала проектирования и разработки React/Next.js фронта.


