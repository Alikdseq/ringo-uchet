## 🧱 Архитектурные решения для нового React/Next.js фронта Ringo Uchet

Документ реализует шаг **W0.3** из `ReactWebFrontendPlan.md`:  
фиксирует выбранный стек, ключевые архитектурные решения и layout‑уровни для фронта.  
Цель: максимальная производительность, предсказуемость и простота сопровождения.

---

## 1. Выбранный стек

### 1.1 Основной фреймворк и язык
- **Next.js 14+ (App Router, React Server Components)**:
  - Используем новую архитектуру `app/` роутера.
  - Серверные компоненты для страниц/данных, клиентские компоненты там, где нужен state/обработчики.
  - Встроенный image/font оптимизатор (`next/image`, `next/font`).
- **React 18**:
  - Concurrent rendering.
  - Suspense для асинхронных данных.
- **TypeScript**:
  - Жёсткая типизация моделей (Order, User, Reports и т.д.), синхронизированная с backend/Flutter моделями.
  - Общие типы в `src/shared/types`.

### 1.2 UI‑слой
- **Tailwind CSS 3.x**:
  - Утилитарные классы для быстрой верстки.
  - Минимальный runtime‑оверхед (всё на этапе сборки).
  - Лёгкая настройка тем и цветовой схемы.
- **Headless UI / Radix‑подход (headless компоненты)**:
  - Компоненты без жёсткого стиля (dialog, menu, listbox, tabs), стилизуем Tailwind’ом.
  - Приоритет — отсутствие тяжёлых CSS‑in‑JS решений и сложных темизаций.
- **Собственная UI‑библиотека поверх Tailwind**:
  - Базовые компоненты в `src/shared/components/ui`: `Button`, `Input`, `Card`, `Badge`, `Tabs`, `Modal`, `Table`, `FormField`.
  - Компоненты статусов (статусы заказов, оплат, техники) повторяют цветовую схему Flutter `StatusColors`.

### 1.3 Работа с данными
- **TanStack Query (React Query) v5**:
  - Отдельный `QueryClient` с глобальными настройками:
    - `staleTime` и `cacheTime` для справочников (equipment/services/materials/clients) — большие значения.
    - Автоматический refetch по фокусу окна для критичных данных (orders).
  - Query‑ключи структурированы: `['orders', filters]`, `['order', id]`, `['catalog', 'equipment']`, `['reports', 'summary', period]`.
  - Встроенный retry и управление ошибками (перехват в одном месте).
- **Разделение server‑state / client‑state**:
  - Вся **серверная** информация (данные backend) — через React Query.
  - Только локальный UI‑state (выбор таба, модалки и т.д.) — в Zustand/Context.

### 1.4 Состояние приложения
- **Zustand** (минимальный глобальный store):
  - Store `useAuthStore`:
    - `user`, `accessToken`, `refreshToken`, `isAuthenticated`, `isLoading`, `error`.
    - Методы: `login`, `logout`, `refreshTokenSafe`, `tryAutoLogin`, `setUser`.
  - Store `useLayoutStore`:
    - Текущий индекс нижней навигации (аналог `navigationIndexProvider`).
    - Открытость сайдбара, тема (light/dark/system).
  - Хранилища маленькие, без сложных вложенных структур.
- **React Context**:
  - Провайдеры для:
    - `QueryClientProvider` (React Query).
    - `AuthContext` (обёртка над Zustand для удобства в компонентах).
    - `ThemeProvider` (если нужна смена темы).

### 1.5 HTTP‑клиент
- **Axios**:
  - Один инстанс в `src/shared/api/httpClient.ts`:
    - `baseURL` из `process.env.NEXT_PUBLIC_API_BASE_URL`.
    - `timeout` 20–45 секунд (повторяет логику Flutter `AppConstants` с учётом VPN).
    - Заголовки:
      - `Content-Type: application/json`.
      - `Accept: application/json`.
      - На web **не** ставим `Accept-Encoding` (это делает браузер).
  - **Interceptors**:
    - Request:
      - Добавление `Authorization: Bearer <accessToken>` из `useAuthStore`.
    - Response:
      - Преобразование ошибок в унифицированный `AppError` (аналог `AppException`):
        - Сетевые ошибки → `network`.
        - Таймауты → `timeout`.
        - 400/401/403/404/422/429/5xx → типизированные ошибки с сообщением и деталями.
      - Если 401 и есть refresh — попытка `refreshTokenSafe`, повтор запроса один раз (аналог AuthInterceptor/RetryInterceptor из Flutter).

---

## 2. Layout‑уровни и структура приложения

### 2.1 RootLayout
- Файл: `src/app/layout.tsx`.
- Ответственности:
  - Подключение глобальных стилей (`globals.css`, Tailwind).
  - Подключение шрифта (`next/font`).
  - Оборачивание всего приложения в провайдеры:
    - `QueryClientProvider`.
    - `AuthProvider` (инициализация auth store, auto‑login/refresh).
    - `ThemeProvider`.
    - Глобальный `ToastProvider` (уведомления об ошибках/успехах).
  - Установка `lang="ru"` (по умолчанию) и поддержки локализации (позже, через `next-intl`/свой механизм).
  - Глобальный `<Suspense>` и fallback‑экран загрузки.

### 2.2 AuthLayout
- Путь: `src/app/(auth)/layout.tsx`.
- Ответственности:
  - Упрощённый layout для страниц логина/регистрации:
    - Центрированный контейнер формы.
    - Фон (градиент/картинка) с логотипом Ringo Uchet.
  - Не содержит навигации по приложению, только бренд и форму.
  - Обработка:
    - Если пользователь уже авторизован — редирект на `/` (guard на уровне клиента или middleware).

### 2.3 AppLayout
- Путь: `src/app/(app)/layout.tsx`.
- Ответственности:
  - Общий layout для всех авторизованных страниц:
    - Верхний AppBar:
      - Текущий заголовок страницы.
      - Кнопка меню (открытие Drawer на мобильных).
      - Быстрые действия (например, кнопка «Создать заявку»).
    - Боковая навигация (desktop):
      - Пункты: Dashboard, Заявки, Номенклатура, Отчёты/Зарплаты, Профиль, Оффлайн‑очередь.
      - Адаптация доступных пунктов в зависимости от роли.
    - Нижняя навигация (mobile):
      - Аналог Flutter‑навигации: иконки и названия разделов.
  - Встраивание:
    - `OfflineBanner` (потеря соединения).
    - Глобальный обработчик ошибок (catch‑boundary).
  - Контентная область:
    - Использует CSS Grid/Flex layout:
      - Sidebar (desktop) / скрытый (mobile).
      - `main` с padding и скроллом.
    - Вложенные страницы (Dashboard, Orders и т.д.) помещаются в `main`.

---

## 3. Производительность и масштабируемость (ключевые принципы)

- **Рендерить только необходимое**:
  - Использовать React Query для мемоизации и дедупликации запросов.
  - Использовать `select` в Query для вытаскивания только нужных полей.
- **Ленивая загрузка тяжёлых модулей**:
  - Отчёты/графики (`/reports/*`) подключать через `next/dynamic` с отложенной загрузкой.
- **Минимум глобального состояния**:
  - Всё, что можно, держать в Query или локальном state компонентов.
  - В Zustand хранить только:
    - Auth (текущий пользователь и токены).
    - Навигацию и базовые UI‑флаги.
- **Повторное использование решений из Flutter**:
  - Логика таймаутов/ретраев/кэша повторяется на уровне React Query + Axios.
  - Цвета статусов и метки (`StatusBadge`) прямо соответствуют `StatusColors` из Flutter.

Эти решения обеспечивают быстрый, предсказуемый и расширяемый фронт, который напрямую повторяет уже проверенную архитектуру мобильного клиента и максимально эффективно использует возможности Next.js.


