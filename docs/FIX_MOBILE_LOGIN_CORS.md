# 🔧 ИСПРАВЛЕНИЕ: Ошибка при входе с мобильного - CORS

## ❌ ПРОБЛЕМА

**CORS разрешает только HTTP, а не HTTPS!**

В `docker-compose.prod.yml`:
```
CORS_ALLOWED_ORIGINS=http://ringoouchet.ru,...  ← Только HTTP!
```

**Но сайт работает по HTTPS!**

---

## ✅ РЕШЕНИЕ

**Я обновил docker-compose.prod.yml, добавив HTTPS origins!**

### ШАГ 1: Перезапустить API

```bash
cd ~/ringo-uchet/backend
docker compose -f docker-compose.prod.yml restart api
```

**Подождите 10 секунд.**

---

### ШАГ 2: Проверка CORS в контейнере

```bash
docker compose -f docker-compose.prod.yml exec api env | grep CORS
```

**Должно показать HTTPS origins!**

---

### ШАГ 3: Проверка на мобильном

**Попробуйте войти с телефона еще раз.**

---

## 📋 ДОПОЛНИТЕЛЬНАЯ ДИАГНОСТИКА

**Если все еще не работает, проверьте логи:**

```bash
docker compose -f ~/ringo-uchet/backend/docker-compose.prod.yml logs api | tail -50
```

**Ищите ошибки при попытке входа.**

---

**Выполните шаг 1 (перезапуск API) и попробуйте войти с телефона!**

