# 📍 ГДЕ РАЗМЕСТИТЬ РЕДИРЕКТ НА HTTPS

## ✅ ОТВЕТ

**В HTTP блоке замените весь блок `location / {` (который раздает статику) на редирект.**

---

## 📝 СТРУКТУРА HTTP БЛОКА

```nginx
server {
    listen 80;
    server_name ringoouchet.ru www.ringoouchet.ru 91.229.90.72;

    # Блок для Certbot (ОСТАВИТЬ)
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # ЭТОТ БЛОК ЗАМЕНИТЬ на редирект ↓
    location / {
        root /var/www/ringo-uchet;  ← УДАЛИТЬ всё это
        index index.html;            ← УДАЛИТЬ
        try_files $uri $uri/ /index.html;  ← УДАЛИТЬ
        ...
    }
    # ↑ Заменить на:
    
    location / {
        return 301 https://$server_name$request_uri;
    }
}
```

---

**Коротко: замените весь блок `location / {` который раздает файлы, на редирект.**

