# 🔍 ПРОВЕРКА: Доступен ли API через домен?

## ❓ ВОПРОС

**Работает ли бэкенд на домене `https://ringoouchet.ru/api/`?**

---

## ✅ ПРОВЕРКА 1: API через домен

**На сервере:**

```bash
curl -k https://ringoouchet.ru/api/health/
```

**Что должно быть:**
- ✅ JSON ответ с `"status": "healthy"`
- ❌ Ошибка 404 или 502 - проблема с Nginx

**Пришлите результат!**

---

## ✅ ПРОВЕРКА 2: Конфигурация Nginx

**На сервере:**

```bash
cat /etc/nginx/sites-available/ringo-uchet | grep -A 10 "location /api/"
```

**Что искать:**
- Должен быть блок `location /api/` который проксирует на `http://localhost:8001`

**Пришлите вывод!**

---

## ✅ ПРОВЕРКА 3: Проверка что API доступен локально

**На сервере:**

```bash
curl http://localhost:8001/api/health/
```

**Должен вернуть JSON!**

**Пришлите результат!**

---

**Выполните все 3 проверки и пришлите результаты!**

