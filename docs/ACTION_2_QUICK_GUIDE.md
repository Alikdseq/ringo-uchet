# 🚀 БЫСТРАЯ ИНСТРУКЦИЯ: ПОДКЛЮЧЕНИЕ К СЕРВЕРУ

## 📍 ДЕЙСТВИЕ 2: Подключение и первичная настройка

---

## ЧАСТЬ 1: Получить IP-адрес сервера (2 минуты)

1. Откройте панель Beget: https://beget.com
2. Войдите в аккаунт
3. Перейдите в раздел **"VPS"** или **"Серверы"**
4. Выберите ваш сервер (Ubuntu 24.04, 3 CPU, 6 GB RAM)
5. **Скопируйте IP-адрес** (формат: `123.45.67.89`)
6. Найдите **пароль root** (в разделе "Пароли" / "Доступ" / "SSH")

**Запишите:**
- IP: `_________________`
- Пароль: `_________________`

---

## ЧАСТЬ 2: Подключиться к серверу (3 минуты)

### На Windows:

1. **Откройте PowerShell:**
   - Нажмите `Windows + R`
   - Введите: `powershell`
   - Нажмите `Enter`

2. **Введите команду подключения:**
   ```powershell
   ssh root@ВАШ_IP_АДРЕС
   ```
   
   **Пример:** Если IP `123.45.67.89`, то:
   ```powershell
   ssh root@123.45.67.89
   ```

3. **При первом подключении:**
   - Напишите: `yes` и нажмите `Enter`

4. **Введите пароль:**
   - Введите пароль root (не будет виден - это нормально!)
   - Нажмите `Enter`

5. **Готово!** Вы увидите: `root@ваш-сервер:~#`

---

## ЧАСТЬ 3: Выполнить настройку (5 минут)

**После подключения выполните команды по порядку:**

### 1. Обновление системы
```bash
sudo apt update && sudo apt upgrade -y
```
⏱️ Займет 2-5 минут

### 2. Установка утилит
```bash
sudo apt install -y curl wget git vim ufw htop
```
⏱️ Займет 1-2 минуты

### 3. Настройка файрвола
```bash
sudo ufw allow 22/tcp
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable
```
При вопросе "Proceed?" → введите: `y`

### 4. Проверка
```bash
sudo ufw status
```
Должно показать: `Status: active`

---

## ✅ ГОТОВО!

Напишите мне: **"Готово, подключился и выполнил настройку"**

Или если возникла проблема - опишите, что произошло.

---

**Время выполнения:** 10-15 минут

