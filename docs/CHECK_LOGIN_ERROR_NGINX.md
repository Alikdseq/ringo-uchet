# 🔍 ПРОВЕРКА: Ошибка при входе с мобильного

## ❌ ПРОБЛЕМА

**Не могу войти с телефона - ошибка сервера.**

**В логах API нет ошибок - проверяем Nginx!**

---

## 📋 ПРОВЕРКА 1: Логи Nginx Access (POST запросы)

```bash
sudo tail -200 /var/log/nginx/ringo-uchet-access.log | grep POST
```

**Ищите POST запросы к `/api/token/` - увидите статус коды (200, 400, 500).**

---

## 📋 ПРОВЕРКА 2: Логи Nginx Error

```bash
sudo tail -100 /var/log/nginx/ringo-uchet-error.log
```

**Ищите ошибки 500, 502, 503.**

---

## 📋 ПРОВЕРКА 3: Мониторинг в реальном времени

**Попробуйте войти с мобильного ПРЯМО СЕЙЧАС:**

**На сервере:**

```bash
sudo tail -f /var/log/nginx/ringo-uchet-access.log | grep -E "POST|token|api"
```

**Затем попробуйте войти с телефона - увидите запрос сразу!**

---

**Выполните проверки 1-2 и отправьте логи!**

