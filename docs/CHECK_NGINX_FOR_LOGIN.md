# 🔍 ПРОВЕРКА ЛОГОВ NGINX ПРИ ВХОДЕ

## 🔍 ENDPOINT ДЛЯ ВХОДА

**Flutter использует:** `/api/token/` (относительный путь `/token/`)

**Полный URL:** `https://ringoouchet.ru/api/token/`

---

## 📋 ПРОВЕРКА 1: Логи Nginx Access (POST запросы)

```bash
sudo tail -200 /var/log/nginx/ringo-uchet-access.log | grep -i "POST\|token\|api"
```

**Ищите POST запросы к `/api/token/` - увидите статус коды!**

---

## 📋 ПРОВЕРКА 2: Логи Nginx Error

```bash
sudo tail -100 /var/log/nginx/ringo-uchet-error.log
```

**Ищите ошибки при POST запросах.**

---

## 📋 ПРОВЕРКА 3: Попробовать войти с мобильного СЕЙЧАС

**Пока мы мониторим логи, попробуйте войти с телефона:**

**На сервере в ОДНОМ терминале:**

```bash
sudo tail -f /var/log/nginx/ringo-uchet-access.log | grep -i "POST\|token"
```

**Затем попробуйте войти с телефона - увидите запрос сразу!**

---

**Выполните проверки 1-2 и отправьте логи!**

