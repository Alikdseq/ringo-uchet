# 🔍 ПОИСК ОШИБКИ ПРИ ВХОДЕ С МОБИЛЬНОГО

## ❌ ПРОБЛЕМА

**В логах API нет ошибок при попытке входа - возможно:**
1. Запрос не доходит до API (проблема в Nginx)
2. Ошибка происходит на клиенте (Flutter)
3. Нужно посмотреть логи в реальном времени

---

## 🔍 ПРОВЕРКА 1: Логи Nginx Error

```bash
sudo tail -100 /var/log/nginx/ringo-uchet-error.log
```

**Ищите ошибки 500, 502, 503 при POST запросах.**

---

## 🔍 ПРОВЕРКА 2: Логи Nginx Access (POST запросы)

```bash
sudo tail -200 /var/log/nginx/ringo-uchet-access.log | grep -i "POST\|auth\|login"
```

**Ищите POST запросы к /api/auth/... и их статус коды.**

---

## 🔍 ПРОВЕРКА 3: Мониторинг в реальном времени

**Откройте 2 терминала:**

**Терминал 1 - Nginx ошибки:**
```bash
sudo tail -f /var/log/nginx/ringo-uchet-error.log
```

**Терминал 2 - API логи:**
```bash
docker compose -f ~/ringo-uchet/backend/docker-compose.prod.yml logs -f api
```

**Затем попробуйте войти с телефона - увидите ошибки сразу!**

---

## 🔍 ПРОВЕРКА 4: Проверить endpoint входа

**Какой endpoint используется? Проверим:**

```bash
curl -X POST https://ringoouchet.ru/api/auth/login/ \
  -H "Content-Type: application/json" \
  -H "Origin: https://ringoouchet.ru" \
  -d '{"phone": "test", "password": "test"}' -v
```

**Что возвращает?**

---

## 🔍 ПРОВЕРКА 5: Проверить CORS в контейнере

```bash
docker compose -f ~/ringo-uchet/backend/docker-compose.prod.yml exec api env | grep CORS
```

**Должны быть HTTPS origins!**

---

**ВЫПОЛНИТЕ ПРОВЕРКИ 1-2 И ОТПРАВЬТЕ ЛОГИ!**

**Или выполните проверку 3 - мониторинг в реальном времени!**

