# 🔧 ИСПРАВЛЕНИЕ: Файлы Flutter Web не в нужной папке

## ❌ ПРОБЛЕМА
После распаковки zip файлы не находятся в `/var/www/ringo-uchet/`

---

## ✅ ПРОВЕРКА 1: Что находится в директории

```bash
ls -la /var/www/ringo-uchet/
```

**Посмотрите, что там есть.**

---

## ✅ ПРОВЕРКА 2: Проверка содержимого домашней директории

```bash
ls -la ~/
```

**Может быть архив распаковался в домашней директории.**

---

## ✅ РЕШЕНИЕ

### Вариант 1: Файлы в ~/web-build/ или подобной папке

```bash
# Проверьте
ls -la ~/web-build/

# Если там файлы, скопируйте их
sudo cp -r ~/web-build/* /var/www/ringo-uchet/
```

### Вариант 2: Файлы распаковались в /var/www/ringo-uchet/web/

```bash
# Проверьте структуру
ls -la /var/www/ringo-uchet/
ls -la /var/www/ringo-uchet/web/

# Если файлы в web/, скопируйте их на уровень выше
sudo cp -r /var/www/ringo-uchet/web/* /var/www/ringo-uchet/
sudo rm -rf /var/www/ringo-uchet/web
```

### Вариант 3: Перераспаковка напрямую в нужную папку

```bash
cd ~
unzip -o web-build.zip -d /var/www/ringo-uchet/

# Если создалась папка web внутри
ls -la /var/www/ringo-uchet/
# Если есть папка web:
sudo mv /var/www/ringo-uchet/web/* /var/www/ringo-uchet/
sudo rmdir /var/www/ringo-uchet/web
```

---

## ✅ ПРОВЕРКА РЕЗУЛЬТАТА

```bash
ls -la /var/www/ringo-uchet/
```

**Должны быть файлы:**
- `index.html`
- `main.dart.js`
- `manifest.json`
- папка `assets/`

---

## 📝 НАСТРОЙКА ПРАВ

После перемещения файлов:

```bash
sudo chown -R www-data:www-data /var/www/ringo-uchet
sudo chmod -R 755 /var/www/ringo-uchet
```

