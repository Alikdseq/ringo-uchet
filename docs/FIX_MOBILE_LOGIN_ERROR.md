# 🔧 ИСПРАВЛЕНИЕ: Ошибка сервера при входе с мобильного

## ❌ ПРОБЛЕМА

- На ноутбуке сайт работает ✅
- На телефоне сайт открывается ✅
- При попытке войти (номер телефона + пароль) - **ошибка сервера** ❌

---

## 🔍 ДИАГНОСТИКА

### ШАГ 1: Проверить логи API

**На сервере:**

```bash
docker compose -f ~/ringo-uchet/backend/docker-compose.prod.yml logs api | tail -50
```

**Ищите ошибки при попытке входа.**

---

### ШАГ 2: Проверить логи Nginx

```bash
sudo tail -50 /var/log/nginx/ringo-uchet-error.log
```

**Ищите ошибки 500 или другие.**

---

### ШАГ 3: Проверить CORS настройки

**На сервере:**

```bash
cd ~/ringo-uchet/backend
cat .env | grep CORS
```

**Убедитесь, что CORS разрешает запросы с мобильных устройств.**

---

### ШАГ 4: Проверить базу данных

**В логах была ошибка:**
```
password authentication failed for user "ringo_user"
```

**Возможно проблема с подключением к БД.**

---

## ✅ ВОЗМОЖНЫЕ ПРИЧИНЫ

1. **CORS не настроен для мобильных** - нужно добавить в CORS_ALLOWED_ORIGINS
2. **Проблема с базой данных** - ошибка аутентификации
3. **Проблема с API endpoint** - неправильный URL
4. **SSL сертификат** - проблемы на мобильном

---

**Выполните шаги 1-2 и отправьте логи - найдем проблему!**

