# 🔍 ПОЛНАЯ ПРОВЕРКА СИСТЕМЫ

## 🎯 ЦЕЛЬ

**Проверить ВСЕ компоненты и убедиться что всё работает!**

---

## ✅ ПРОВЕРКА 1: Работает ли Backend на сервере?

### ШАГ 1: Проверить запущены ли контейнеры

```bash
cd /root/ringo-uchet/backend
docker compose -f docker-compose.prod.yml ps
```

**Должны быть запущены:**
- ✅ `api` (Django)
- ✅ `db` (PostgreSQL)
- ✅ `redis` (Redis)
- ✅ `minio` (MinIO)

**Если не запущены - запустите:**

```bash
docker compose -f docker-compose.prod.yml up -d
```

---

### ШАГ 2: Проверить логи API

```bash
docker compose -f docker-compose.prod.yml logs api --tail=50
```

**Ищите ошибки!**

---

### ШАГ 3: Проверить API локально на сервере

```bash
curl http://localhost:8001/api/health/
```

**Должен вернуть JSON с `"status": "ok"`**

---

## ✅ ПРОВЕРКА 2: Есть ли файлы Flutter Web?

### ШАГ 1: Проверить файлы

```bash
ls -la /var/www/ringo-uchet/
```

**Должны быть:**
- ✅ `index.html`
- ✅ `main.dart.js`
- ✅ `manifest.json`

**Если нет - нужно восстановить!**

---

## ✅ ПРОВЕРКА 3: Работает ли Nginx?

### ШАГ 1: Проверить конфигурацию

```bash
sudo nginx -t
```

**Должно быть:** `syntax is ok` и `test is successful`

---

### ШАГ 2: Проверить статус Nginx

```bash
sudo systemctl status nginx
```

**Должно быть:** `active (running)`

---

### ШАГ 3: Проверить API через домен

```bash
curl -k https://ringoouchet.ru/api/health/
```

**Должен вернуть JSON**

---

## ✅ ПРОВЕРКА 4: Работает ли сайт?

### ШАГ 1: Проверить главную страницу

```bash
curl -k https://ringoouchet.ru/ | head -20
```

**Должен вернуть HTML (index.html)**

---

## 📋 ЧЕКЛИСТ ПРОБЛЕМ И РЕШЕНИЙ

### ❌ Проблема 1: Backend не запущен

**Решение:**

```bash
cd /root/ringo-uchet/backend
docker compose -f docker-compose.prod.yml up -d
docker compose -f docker-compose.prod.yml logs api --tail=50
```

---

### ❌ Проблема 2: Файлы Flutter Web отсутствуют

**Решение:**
- См. инструкцию `RESTORE_FLUTTER_FILES.md`

---

### ❌ Проблема 3: API недоступен через домен

**Решение:**
- Проверить Nginx конфигурацию
- Проверить что `location /api/` правильно настроен

---

**Выполните все проверки и пришлите результаты!**

