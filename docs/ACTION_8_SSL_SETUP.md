# ✅ ДЕЙСТВИЕ 8: НАСТРОЙКА ДОМЕНА И SSL (HTTPS)

## 🎯 ЦЕЛЬ
Привязать домен к серверу Beget, заменить IP на домен в настройках и получить SSL сертификат через Let's Encrypt для работы через HTTPS.

---

## 📋 ЧАСТЬ 1: НАСТРОЙКА ДОМЕНА ДЛЯ СЕРВЕРА BEGET

### 🎯 ЦЕЛЬ ЧАСТИ 1
Привязать домен к серверу Beget через DNS записи.

---

### ШАГ 1.1: УЗНАТЬ IP АДРЕС СЕРВЕРА

**На сервере выполните:**

```bash
hostname -I
```

**Или посмотрите IP в панели Beget:**
1. Откройте панель Beget: https://beget.com
2. Перейдите в раздел **"VPS"** или **"Серверы"**
3. Найдите ваш сервер
4. **Скопируйте IP адрес** (формат: `123.45.67.89`)

**Запишите IP:** `_________________`

---

### ШАГ 1.2: ОТКРЫТЬ ПАНЕЛЬ УПРАВЛЕНИЯ ДОМЕНОМ

**Где у вас зарегистрирован домен?**

#### Вариант А: Домен зарегистрирован в Beget

1. **Откройте панель Beget:** https://beget.com
2. **Войдите в аккаунт**
3. **Перейдите в раздел:** **"Домены"** или **"DNS"**
4. **Найдите ваш домен** в списке
5. **Нажмите на домен** или кнопку **"Управление DNS"** / **"Настройка DNS"**

#### Вариант Б: Домен зарегистрирован в другом регистраторе

1. **Откройте панель вашего регистратора домена** (например: REG.RU, Timeweb, GoDaddy и т.д.)
2. **Войдите в аккаунт**
3. **Найдите раздел:** **"Домены"**, **"Мои домены"** или **"DNS управление"**
4. **Выберите ваш домен**
5. **Перейдите в раздел DNS записей**

---

### ШАГ 1.3: СОЗДАНИЕ DNS ЗАПИСЕЙ

**Нужно создать A записи, которые свяжут домен с IP сервера.**

#### 1.3.1 Основная A запись (для домена без www)

**Найдите кнопку:** **"Добавить запись"**, **"Создать запись"** или **"+"**

**Заполните:**
- **Тип записи:** `A` (или выберите из списка)
- **Имя/Хост:** `@` или оставьте **пустым** (это означает сам домен)
- **Значение/IP:** `ВАШ_IP_АДРЕС` (тот, что записали в шаге 1.1)
- **TTL:** `3600` или `Auto` (можно оставить по умолчанию)

**Нажмите:** **"Сохранить"**, **"Создать"** или **"Добавить"**

---

#### 1.3.2 A запись для www (опционально, но рекомендуется)

**Создайте еще одну A запись:**

- **Тип записи:** `A`
- **Имя/Хост:** `www` (буквально напишите "www")
- **Значение/IP:** `ВАШ_IP_АДРЕС` (тот же IP)
- **TTL:** `3600` или `Auto`

**Нажмите:** **"Сохранить"**

---

#### 1.3.3 Примеры заполнения для разных панелей

**Для Beget:**
```
Тип: A
Имя: @
Значение: 123.45.67.89
TTL: 3600
```

**Для REG.RU:**
```
Тип записи: A
Поддомен: @
IP-адрес: 123.45.67.89
TTL: 3600
```

**Для Timeweb:**
```
Тип: A
Имя: @
IP: 123.45.67.89
TTL: 3600
```

---

### ШАГ 1.4: ПРОВЕРКА DNS ЗАПИСЕЙ

**После создания записей подождите 5-30 минут** (DNS записи распространяются).

**Затем проверьте на сервере:**

```bash
dig ваш-домен.ru
# или
nslookup ваш-домен.ru
# или
host ваш-домен.ru
```

**Должен показать ваш IP адрес сервера.**

**Пример правильного ответа:**
```
ваш-домен.ru.    3600    IN    A    123.45.67.89
```

**Если показывает другой IP или ошибку:**
- Подождите еще 10-30 минут (DNS может распространяться до 24 часов)
- Проверьте, правильно ли указан IP в DNS записи
- Убедитесь, что сохранили записи

---

### ШАГ 1.5: ПРОВЕРКА ЧЕРЕЗ БРАУЗЕР

**Попробуйте открыть в браузере:**

```
http://ваш-домен.ru
```

**Если открывается та же страница, что и по IP** - DNS работает! ✅

**Если не открывается:**
- Подождите еще немного (DNS распространяется)
- Проверьте DNS записи еще раз
- Попробуйте через другой DNS сервер: https://dnschecker.org

---

## 📋 ЧАСТЬ 2: ЗАМЕНА IP НА ДОМЕН В КОНФИГУРАЦИЯХ

**Теперь нужно заменить IP адрес на домен в настройках приложения.**

---

### ШАГ 2.1: ЗАМЕНА В BACKEND .ENV ФАЙЛЕ

**На сервере:**

```bash
cd ~/ringo-uchet/backend
nano .env
```

**Найдите строку с `DJANGO_ALLOWED_HOSTS` или `ALLOWED_HOSTS` и измените:**

**Было (с IP):**
```env
DJANGO_ALLOWED_HOSTS=123.45.67.89
# или
ALLOWED_HOSTS=123.45.67.89
```

**Стало (с доменом):**
```env
DJANGO_ALLOWED_HOSTS=ваш-домен.ru,www.ваш-домен.ru,123.45.67.89
# или
ALLOWED_HOSTS=ваш-домен.ru,www.ваш-домен.ru,123.45.67.89
```

**Важно:** Оставьте IP тоже, на случай если домен временно не работает.

**Также проверьте `CORS_ALLOWED_ORIGINS` (если есть):**

```env
CORS_ALLOWED_ORIGINS=http://ваш-домен.ru,http://www.ваш-домен.ru,http://123.45.67.89
```

**Сохраните:** `Ctrl + O`, `Enter`, `Ctrl + X`

**Перезапустите API:**

```bash
docker compose -f docker-compose.prod.yml restart api
```

---

### ШАГ 2.2: ЗАМЕНА В FLUTTER КОНФИГУРАЦИИ

**На вашем компьютере откройте файл:**

`mobile/lib/core/config/app_config.dart`

**Найдите метод `static AppConfig get prod` и измените `apiBaseUrl`:**

**Было (с IP):**
```dart
static AppConfig get prod => const AppConfig(
  flavor: AppFlavor.prod,
  apiBaseUrl: 'http://123.45.67.89:8001',
  // ...
);
```

**Стало (с доменом, пока HTTP, потом будет HTTPS):**
```dart
static AppConfig get prod => const AppConfig(
  flavor: AppFlavor.prod,
  apiBaseUrl: 'http://ваш-домен.ru',
  // ...
);
```

**Важно:** Пока используем `http://`, после настройки SSL изменим на `https://`.

**Сохраните файл.**

**Пересоберите Flutter Web:**

```powershell
cd C:\ringo-uchet\mobile
flutter clean
flutter build web --release --base-href /
```

**Скопируйте на сервер:**

```powershell
cd build
Compress-Archive -Path web\* -DestinationPath web-build.zip
scp web-build.zip root@ВАШ_IP:~/web-build.zip
```

**На сервере распакуйте:**

```bash
cd ~
unzip -o web-build.zip -d /var/www/ringo-uchet/
sudo chown -R www-data:www-data /var/www/ringo-uchet
```

---

### ШАГ 2.3: ПРОВЕРКА РАБОТЫ С ДОМЕНОМ

**После замены IP на домен:**

1. **Проверьте фронтенд:**
   ```
   http://ваш-домен.ru
   ```
   Должна загрузиться главная страница.

2. **Проверьте API:**
   ```bash
   curl http://ваш-домен.ru/api/health/
   ```
   Должен вернуть ответ (даже если 301 - это нормально).

---

## 📋 ЧАСТЬ 3: НАСТРОЙКА SSL СЕРТИФИКАТА

**Теперь, когда домен настроен, настраиваем SSL.**

---

### ШАГ 3.1: УБЕДИТЕСЬ, ЧТО ДОМЕН РАБОТАЕТ

**Перед получением SSL сертификата домен ДОЛЖЕН указывать на сервер!**

**Проверьте:**

```bash
dig ваш-домен.ru +short
```

**Должен показать ваш IP адрес.**

**Также проверьте в браузере:**
```
http://ваш-домен.ru
```

**Если открывается - можно продолжать! ✅**

---

### ШАГ 3.2: ОБНОВЛЕНИЕ КОНФИГУРАЦИИ NGINX ДЛЯ SSL

**Создайте новую конфигурацию:**

```bash
sudo nano /etc/nginx/sites-available/ringo-uchet
```

**Замените содержимое на:**

```nginx
# HTTP сервер - редирект на HTTPS (после получения сертификата)
server {
    listen 80;
    listen [::]:80;
    server_name ваш-домен.ru www.ваш-домен.ru ВАШ_IP;

    # Временно для получения сертификата - раскомментируйте после получения
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # После получения сертификата - раскомментируйте редирект:
    # return 301 https://$server_name$request_uri;
}

# HTTPS сервер
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name ваш-домен.ru www.ваш-домен.ru;

    # SSL сертификаты (будут созданы Certbot)
    ssl_certificate /etc/letsencrypt/live/ваш-домен.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/ваш-домен.ru/privkey.pem;
    
    # SSL настройки
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # Логи
    access_log /var/log/nginx/ringo-uchet-access.log;
    error_log /var/log/nginx/ringo-uchet-error.log;

    # Максимальный размер загружаемых файлов
    client_max_body_size 100M;

    # Статические файлы Flutter Web приложения
    location / {
        root /var/www/ringo-uchet;
        index index.html;
        try_files $uri $uri/ /index.html;
        
        # Кэширование статических файлов
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|wasm)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
        
        # Без кэширования для HTML
        location ~* \.html$ {
            expires -1;
            add_header Cache-Control "no-store, no-cache, must-revalidate";
        }
    }

    # API проксирование к Django
    location /api/ {
        proxy_pass http://127.0.0.1:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $server_name;
        
        # Таймауты
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        
        # WebSocket поддержка
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Безопасность заголовков
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
    
    # PWA поддержка
    add_header Service-Worker-Allowed "/" always;
    
    # HSTS (строгая безопасность)
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
}
```

**ВАЖНО:** Замените `ваш-домен.ru` на ваш реальный домен и `ВАШ_IP` на IP сервера!

**Сохраните:** `Ctrl + O`, `Enter`, `Ctrl + X`

---

### ШАГ 3.3: СОЗДАНИЕ ДИРЕКТОРИИ ДЛЯ CERTBOT

```bash
sudo mkdir -p /var/www/certbot
sudo chown -R www-data:www-data /var/www/certbot
```

---

### ШАГ 3.4: ПРОВЕРКА КОНФИГУРАЦИИ NGINX

```bash
sudo nginx -t
```

**Должно показать:** `syntax is ok` и `test is successful`

**Если есть ошибки - исправьте их перед продолжением!**

---

### ШАГ 3.5: ЗАПУСК NGINX

```bash
sudo systemctl reload nginx
```

---

### ШАГ 3.6: ПОЛУЧЕНИЕ SSL СЕРТИФИКАТА

**Временно остановите Nginx (Certbot запустит свой веб-сервер):**

```bash
sudo systemctl stop nginx
```

**Получите сертификат:**

```bash
sudo certbot certonly --standalone \
  --email ваш-email@example.com \
  --agree-tos \
  --no-eff-email \
  -d ваш-домен.ru \
  -d www.ваш-домен.ru
```

**Важно:**
- Замените `ваш-email@example.com` на ваш email
- Замените `ваш-домен.ru` на ваш реальный домен
- Домен должен указывать на ваш сервер!

**Запустите Nginx обратно:**

```bash
sudo systemctl start nginx
```

---

### ШАГ 3.7: ВКЛЮЧЕНИЕ HTTPS РЕДИРЕКТА

**Обновите конфигурацию Nginx:**

```bash
sudo nano /etc/nginx/sites-available/ringo-uchet
```

**Раскомментируйте редирект в HTTP блоке:**

```nginx
server {
    listen 80;
    listen [::]:80;
    server_name ваш-домен.ru www.ваш-домен.ru;

    # Редирект на HTTPS
    return 301 https://$server_name$request_uri;
}
```

**Сохраните:** `Ctrl + O`, `Enter`, `Ctrl + X`

**Проверка и перезагрузка:**

```bash
sudo nginx -t
sudo systemctl reload nginx
```

---

### ШАГ 3.8: ОБНОВЛЕНИЕ FLUTTER КОНФИГУРАЦИИ НА HTTPS

**На вашем компьютере откройте:**

`mobile/lib/core/config/app_config.dart`

**Измените `apiBaseUrl` на HTTPS:**

```dart
static AppConfig get prod => const AppConfig(
  flavor: AppFlavor.prod,
  apiBaseUrl: 'https://ваш-домен.ru',  // ИЗМЕНИТЕ НА HTTPS
  apiVersion: 'v1',
  enableLogging: false,
  enableCrashlytics: true,
  appName: 'Ringo Uchet',
  packageName: 'com.ringo.prod',
);
```

**Пересоберите и загрузите на сервер** (как в Шаге 2.2).

---

### ШАГ 3.9: НАСТРОЙКА АВТООБНОВЛЕНИЯ СЕРТИФИКАТА

**Проверка автообновления:**

```bash
sudo certbot renew --dry-run
```

**Должно показать успешное обновление.**

**Настройте cron для автоматического обновления:**

```bash
sudo crontab -e
```

**Добавьте строку:**

```
0 0 * * * certbot renew --quiet --post-hook "systemctl reload nginx"
```

**Сохраните:** `Ctrl + O`, `Enter`, `Ctrl + X`

---

## ✅ ФИНАЛЬНАЯ ПРОВЕРКА

1. **Откройте в браузере:**
   ```
   https://ваш-домен.ru
   ```

2. **Проверьте сертификат:**
   - Должен быть зеленый замочек
   - Без предупреждений

3. **Проверка API:**
   ```bash
   curl https://ваш-домен.ru/api/health/
   ```

4. **Проверка редиректа:**
   - Откройте `http://ваш-домен.ru`
   - Должен автоматически редиректить на `https://ваш-домен.ru`

---

## 📝 КРАТКИЙ ЧЕКЛИСТ

- [ ] DNS записи созданы (A запись для домена)
- [ ] DNS записи созданы (A запись для www)
- [ ] DNS проверен (dig/nslookup показывает правильный IP)
- [ ] Домен открывается в браузере
- [ ] .env файл обновлен (ALLOWED_HOSTS с доменом)
- [ ] Flutter конфигурация обновлена (apiBaseUrl с доменом)
- [ ] Flutter пересобран и загружен на сервер
- [ ] Nginx конфигурация обновлена (для SSL)
- [ ] SSL сертификат получен через Certbot
- [ ] HTTPS редирект включен
- [ ] Сайт открывается по HTTPS
- [ ] Автообновление сертификата настроено

---

## ⏭️ СЛЕДУЮЩИЙ ШАГ

**После настройки напишите:**
- ✅ **"Готово, домен и SSL настроены"** - перейдем к финальной проверке

---

**Статус:** ⏳ Настройка домена и SSL

**Время выполнения:** 20-30 минут

**Требования:** Домен должен быть зарегистрирован
