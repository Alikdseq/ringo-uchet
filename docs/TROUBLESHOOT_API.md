# 🔧 ДИАГНОСТИКА: API не отвечает

## ❌ ПРОБЛЕМА
`curl http://localhost:8001/api/health/` ничего не возвращает.

---

## ✅ ПРОВЕРКА 1: Статус контейнеров

```bash
docker compose -f docker-compose.prod.yml ps
```

**Что должно быть:**
- Все контейнеры должны быть `Up` или `healthy`
- Если какой-то контейнер не работает - это проблема

---

## ✅ ПРОВЕРКА 2: Логи API контейнера

```bash
docker compose -f docker-compose.prod.yml logs api
```

**Что искать:**
- Ошибки (ERROR, Exception)
- Сообщения о запуске
- Проблемы с подключением к БД

---

## ✅ ПРОВЕРКА 3: Запущен ли контейнер API

```bash
docker ps | grep api
```

**Должен показать контейнер с именем содержащим "api"**

---

## ✅ ПРОВЕРКА 4: Порт 8001 доступен

```bash
netstat -tulpn | grep 8001
# или
ss -tulpn | grep 8001
```

**Должен показать, что порт 8001 слушается**

---

## ✅ ПРОВЕРКА 5: Прямой доступ к контейнеру

```bash
docker compose -f docker-compose.prod.yml exec api curl http://localhost:8000/api/health/
```

**Если это работает, значит проблема с портом 8001**

---

## 🔧 ВОЗМОЖНЫЕ РЕШЕНИЯ

### Решение 1: Контейнер не запустился

```bash
# Перезапустить контейнер
docker compose -f docker-compose.prod.yml restart api

# Или пересобрать и запустить
docker compose -f docker-compose.prod.yml up -d --build api
```

### Решение 2: Ошибка в логах

Посмотрите логи и исправьте ошибку:
```bash
docker compose -f docker-compose.prod.yml logs api
```

### Решение 3: Проблема с БД

Проверьте, запущена ли БД:
```bash
docker compose -f docker-compose.prod.yml ps db
```

---

## 📋 БЫСТРАЯ ПРОВЕРКА

Выполните все команды по порядку и сообщите результаты:

1. `docker compose -f docker-compose.prod.yml ps`
2. `docker compose -f docker-compose.prod.yml logs api | tail -20`

