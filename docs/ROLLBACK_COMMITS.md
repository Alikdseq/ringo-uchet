# 🔄 ОТКАТ КОММИТОВ - ВОЗВРАТ К ПРЕДЫДУЩЕМУ СОСТОЯНИЮ

## ⚠️ ВАЖНО ПЕРЕД ОТКАТОМ

**Сначала проверьте какие коммиты были сделаны сегодня!**

---

## 📋 ШАГ 1: Посмотреть историю коммитов

### На вашем компьютере (PowerShell):

```powershell
cd C:\ringo-uchet\backend
git log --oneline --all -20
```

### Или более подробно:

```powershell
cd C:\ringo-uchet\backend
git log --all --graph --pretty=format:'%h - %an, %ar : %s' -20
```

### Что вы увидите:
- Список последних коммитов
- Хеш коммита (короткий ID)
- Автор и дата
- Сообщение коммита

**Найдите коммит, ДО которого нужно вернуться** (запомните его хеш, например `a762b68`)

---

## 📋 ШАГ 2: Проверить какие файлы изменены в сегодняшних коммитах

### Посмотреть изменения в последних коммитах:

```powershell
cd C:\ringo-uchet\backend
git log --oneline -5
git show HEAD --stat
git show HEAD~1 --stat
```

Это покажет какие файлы были изменены.

---

## 🔄 ВАРИАНТ 1: Откатить последние коммиты (сохранить изменения в рабочей директории)

### Если вы хотите отменить коммиты, но оставить изменения в файлах:

```powershell
cd C:\ringo-uchet\backend

# Откатить последний коммит (изменения останутся в файлах)
git reset --soft HEAD~1

# Или откатить несколько коммитов (например, 2 последних)
git reset --soft HEAD~2

# Или откатить до конкретного коммита
git reset --soft a762b68
```

**Что произойдет:**
- Коммиты отменятся
- Изменения останутся в файлах (как будто вы их только что отредактировали)
- Вы сможете заново закоммитить или отменить изменения

---

## 🔄 ВАРИАНТ 2: Полностью удалить последние коммиты и изменения

### Если вы хотите полностью удалить коммиты и все изменения:

```powershell
cd C:\ringo-uchet\backend

# Откатить последний коммит (изменения удалятся)
git reset --hard HEAD~1

# Или откатить несколько коммитов (например, 2 последних)
git reset --hard HEAD~2

# Или откатить до конкретного коммита
git reset --hard a762b68
```

**⚠️ ВНИМАНИЕ:** Это удалит все изменения! Используйте только если уверены!

---

## 🔄 ВАРИАНТ 3: Откатить до конкретного коммита (рекомендуется)

### Самый безопасный способ:

```powershell
cd C:\ringo-uchet\backend

# 1. Посмотреть историю и найти нужный коммит
git log --oneline -10

# 2. Скопировать хеш нужного коммита (например: a762b68)

# 3. Откатиться к этому коммиту (сохранить изменения)
git reset --soft a762b68

# ИЛИ полностью удалить изменения
git reset --hard a762b68
```

---

## 🔄 ВАРИАНТ 4: Создать новый коммит, который отменяет изменения

### Если коммиты уже запушены в репозиторий:

```powershell
cd C:\ringo-uchet\backend

# Откатить изменения через новый коммит
git revert HEAD
git revert HEAD~1

# Или откатить несколько коммитов сразу
git revert HEAD~2..HEAD
```

**Что произойдет:**
- Создадутся новые коммиты, которые отменяют изменения
- История сохранится
- Безопасно для уже запушенных коммитов

---

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Проверить текущее состояние

```powershell
cd C:\ringo-uchet\backend
git status
git log --oneline -10
```

### Шаг 2: Создать резервную ветку (на всякий случай)

```powershell
cd C:\ringo-uchet\backend
git branch backup-before-rollback
```

Теперь у вас есть резервная копия текущего состояния.

### Шаг 3: Посмотреть какие коммиты нужно откатить

```powershell
cd C:\ringo-uchet\backend
git log --oneline --all --graph -20
```

Найдите коммит, ДО которого нужно вернуться.

### Шаг 4: Откатить коммиты

**Если коммиты НЕ запушены (только локально):**

```powershell
cd C:\ringo-uchet\backend

# Откатить последний коммит (изменения останутся в файлах)
git reset --soft HEAD~1

# Или откатить 2 последних коммита
git reset --soft HEAD~2

# Или откатить до конкретного коммита
git reset --soft a762b68
```

**Если коммиты УЖЕ запушены:**

```powershell
cd C:\ringo-uchet\backend

# Создать коммит, который отменяет изменения
git revert HEAD
```

### Шаг 5: Проверить что получилось

```powershell
cd C:\ringo-uchet\backend
git status
git log --oneline -5
```

### Шаг 6: Если нужно удалить изменения из файлов

```powershell
cd C:\ringo-uchet\backend

# Отменить все изменения в файлах
git checkout -- .

# Или отменить конкретный файл
git checkout -- docker-compose.prod.yml
```

---

## 🎯 КОНКРЕТНЫЙ ПРИМЕР

### Если вы хотите откатить последние 2 коммита:

```powershell
# 1. Перейти в папку
cd C:\ringo-uchet\backend

# 2. Создать резервную копию
git branch backup-$(Get-Date -Format "yyyyMMdd-HHmmss")

# 3. Посмотреть последние коммиты
git log --oneline -5

# 4. Откатить 2 последних коммита (изменения останутся)
git reset --soft HEAD~2

# 5. Проверить статус
git status

# 6. Если нужно удалить изменения из файлов
git reset --hard HEAD
```

---

## ⚠️ ЧТО ДЕЛАТЬ ЕСЛИ ОШИБКА

### Если откатили слишком далеко:

```powershell
cd C:\ringo-uchet\backend

# Вернуться к резервной ветке
git checkout backup-before-rollback

# Или найти нужный коммит в логе
git reflog

# Вернуться к нужному коммиту
git reset --hard <хеш_коммита>
```

### Если нужно восстановить удаленные изменения:

```powershell
cd C:\ringo-uchet\backend

# Посмотреть историю всех действий
git reflog

# Вернуться к нужному состоянию
git reset --hard HEAD@{1}
```

---

## 📋 БЫСТРАЯ КОМАНДА (откатить последний коммит)

```powershell
cd C:\ringo-uchet\backend && git branch backup-rollback && git reset --soft HEAD~1 && git status
```

---

## ❓ ЧАСТЫЕ ВОПРОСЫ

### Вопрос: Какой вариант выбрать?

**Ответ:**
- Если коммиты **НЕ запушены** → используйте `git reset --soft`
- Если коммиты **УЖЕ запушены** → используйте `git revert`
- Если хотите **полностью удалить изменения** → используйте `git reset --hard`

### Вопрос: Как узнать сколько коммитов откатить?

**Ответ:**
```powershell
git log --oneline --since="today"
```
Покажет все коммиты сделанные сегодня.

### Вопрос: Можно ли вернуть откаченные коммиты?

**Ответ:**
Да! Используйте `git reflog` чтобы найти их и вернуться.

---

## 🚨 ВАЖНО

1. **Сначала создайте резервную ветку** - на всякий случай
2. **Проверьте какие коммиты откатываете** - посмотрите историю
3. **Если коммиты запушены** - используйте `git revert` вместо `git reset`

---

**Начните с ШАГА 1 - посмотрите историю коммитов!**

