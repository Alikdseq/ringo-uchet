# 🔍 ПРОВЕРКА: API доступен через домен?

## ❓ ВОПРОС

**Работает ли бэкенд через `https://ringoouchet.ru/api/`?**

---

## ✅ ПРОВЕРКА 1: API через домен (HTTPS)

**На сервере:**

```bash
curl -k https://ringoouchet.ru/api/health/
```

**Пришлите результат!**

---

## ✅ ПРОВЕРКА 2: API через домен (HTTP - редирект)

**На сервере:**

```bash
curl -L http://ringoouchet.ru/api/health/
```

**Пришлите результат!**

---

## ✅ ПРОВЕРКА 3: API локально на сервере

**На сервере:**

```bash
curl http://localhost:8001/api/health/
```

**Должен вернуть JSON!**

**Пришлите результат!**

---

## ✅ ПРОВЕРКА 4: Конфигурация Nginx

**На сервере:**

```bash
sudo cat /etc/nginx/sites-available/ringo-uchet | grep -A 15 "location /api/"
```

**Что искать:**
- Должен быть блок который проксирует `/api/` на `http://localhost:8001` или `http://127.0.0.1:8001`

**Пришлите вывод!**

---

## ✅ ПРОВЕРКА 5: Логи Nginx

**На сервере:**

```bash
sudo tail -20 /var/log/nginx/ringo-uchet-access.log
```

**Ищите запросы к `/api/`**

---

**Выполните все проверки и пришлите результаты!**

