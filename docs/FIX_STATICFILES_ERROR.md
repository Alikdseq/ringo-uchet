# 🔧 ИСПРАВЛЕНИЕ ОШИБКИ STATICFILES READ-ONLY

## 🔴 ПРОБЛЕМА

**Ошибка:** `OSError: [Errno 30] Read-only file system: '/app/ringo_backend/staticfiles'`

**Причина:** `STATIC_ROOT` указывает на `/app/ringo_backend/staticfiles`, но эта папка read-only (смонтирована как `:ro`). Django не может создать директорию для статических файлов.

---

## ✅ ИСПРАВЛЕНИЕ

**Файл:** `backend/ringo_backend/settings/prod.py`

**Изменение:** Переопределен `STATIC_ROOT` чтобы использовать writable volume `/app/staticfiles` вместо read-only папки.

---

## 📋 ПРОВЕРКА НА СЕРВЕРЕ

**На сервере:**

```bash
cd ~/ringo-uchet/backend

# Обновить код
git pull origin master

# Перезапустить контейнеры
docker compose -f docker-compose.prod.yml restart api

# Подождать запуска
sleep 10

# Попробовать collectstatic снова
docker compose -f docker-compose.prod.yml exec api python manage.py collectstatic --noinput
```

**Должно работать без ошибок!**

---

## ✅ ПРОВЕРКА ЧТО ИСПРАВЛЕНО

**На сервере:**

```bash
# Проверить что staticfiles созданы
ls -la staticfiles/

# Должны быть файлы CSS, JS и т.д.
```

---

**Ошибка исправлена! Обновите код на сервере и попробуйте снова.** 🚀

