# 🔧 ИСПРАВЛЕНИЕ: CORS - добавить HTTPS origins

## ❌ ПРОБЛЕМА

**CORS разрешает только HTTP origins, а сайт работает по HTTPS!**

**В docker-compose.prod.yml:**
```
CORS_ALLOWED_ORIGINS=http://ringoouchet.ru,...  ← Только HTTP!
```

---

## ✅ РЕШЕНИЕ

**Я обновил docker-compose.prod.yml!**

**Теперь нужно:**

### ШАГ 1: Перезапустить API

```bash
cd ~/ringo-uchet/backend
docker compose -f docker-compose.prod.yml restart api
```

**Подождите 10 секунд.**

---

### ШАГ 2: Проверка

**Попробуйте войти с телефона еще раз.**

---

### ШАГ 3: Если все еще не работает - проверить логи

```bash
docker compose -f ~/ringo-uchet/backend/docker-compose.prod.yml logs api | tail -100
```

**Ищите ошибки при попытке входа.**

---

**Выполните шаг 1 и попробуйте войти!**

