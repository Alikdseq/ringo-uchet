# ✅ ИСПРАВЛЕНИЕ: Flutter Web Renderer

## ❌ ПРОБЛЕМА

**Ошибка:** `Could not find an option named "--web-renderer"`

**Причина:** В Flutter 3.x+ опция `--web-renderer` была удалена из командной строки.

---

## ✅ РЕШЕНИЕ

В новых версиях Flutter (3.x+) renderer указывается **не через флаг командной строки**, а через **конфигурацию в `index.html`**.

### Конфигурация уже добавлена в `mobile/web/index.html`:

```html
<script>
  window.flutterConfiguration = {
    renderer: "html"
  };
</script>
```

### Правильная команда сборки:

```powershell
cd C:\ringo-uchet\mobile
flutter build web --release --base-href /
```

**БЕЗ флага `--web-renderer`!**

Flutter автоматически прочитает конфигурацию из `index.html` и использует HTML renderer.

---

## ✅ ПРОВЕРКА

После сборки проверьте что HTML renderer используется:

1. Откройте `build/web/index.html`
2. Найдите строку: `renderer: "html"`
3. Должна быть там

Также проверьте размер сборки - должен быть ~6-7 MB вместо 31 MB.

---

## 📋 ИТОГО

- ✅ Конфигурация HTML renderer уже в `index.html`
- ✅ Команда сборки: `flutter build web --release --base-href /`
- ✅ БЕЗ флага `--web-renderer`
- ✅ Flutter автоматически использует HTML renderer

**Все готово к сборке!**

