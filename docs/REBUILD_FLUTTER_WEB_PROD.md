# 🔧 ПЕРЕСБОРКА: Flutter Web с PROD конфигурацией

## ✅ ИСПРАВЛЕНО

**Изменен `main_web.dart` - теперь использует `AppConfig.prod` вместо `AppConfig.dev`!**

---

## 📋 ШАГ 1: Пересобрать Flutter Web

**На вашем компьютере (PowerShell):**

```powershell
cd C:\ringo-uchet\mobile
flutter clean
flutter build web --release --base-href /
```

⏱️ **Займет 3-5 минут**

**Дождитесь окончания сборки!**

---

## 📋 ШАГ 2: Создать архив

**На вашем компьютере:**

```powershell
cd C:\ringo-uchet\mobile\build
Compress-Archive -Path web\* -DestinationPath web-build.zip -Force
```

---

## 📋 ШАГ 3: Загрузить на сервер

**На вашем компьютере:**

```powershell
scp web-build.zip root@91.229.90.72:~/web-build.zip
```

**Введите пароль root когда попросит.**

---

## 📋 ШАГ 4: На сервере - обновить файлы

**На сервере выполните ВСЮ команду:**

```bash
cd ~ && rm -rf /var/www/ringo-uchet/* && unzip -o web-build.zip -d /var/www/ringo-uchet/ && mv /var/www/ringo-uchet/web/* /var/www/ringo-uchet/ && rm -rf /var/www/ringo-uchet/web && sudo chown -R www-data:www-data /var/www/ringo-uchet && sudo chmod -R 755 /var/www/ringo-uchet && ls -la /var/www/ringo-uchet/index.html
```

---

## 📋 ШАГ 5: Проверка

**На сервере:**

```bash
ls -la /var/www/ringo-uchet/ | head -10
```

**Должны быть файлы:**
- ✅ `index.html`
- ✅ `main.dart.js`
- ✅ `manifest.json`

---

## 📋 ШАГ 6: Очистить кеш на телефоне

**На телефоне:**
1. Откройте настройки браузера
2. Найдите "Данные сайтов" или "Очистить данные"
3. Очистите данные для `ringoouchet.ru`
4. Или откройте сайт в режиме инкогнито

---

## ✅ ПОСЛЕ ЭТОГО

**Попробуйте войти на телефоне - должно работать!**

**Начнем с ШАГА 1 - пересоберите Flutter Web!**

