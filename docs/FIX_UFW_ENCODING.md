# 🔧 ИСПРАВЛЕНИЕ: Проблема с кодировкой при включении UFW

## ❌ ПРОБЛЕМА

При выполнении команды `sudo ufw enable` возникает ошибка:
```
UnicodeDecodeError: 'utf-8' codec can't decode byte 0xd0 in position 0
```

**Причина:** Windows PowerShell отправляет ответ в неправильной кодировке, и UFW не может его прочитать.

---

## ✅ РЕШЕНИЕ (3 способа)

### СПОСОБ 1: Использовать флаг --force (САМЫЙ ПРОСТОЙ)

**Выполните эту команду:**

```bash
echo "y" | sudo ufw --force enable
```

Или еще проще:

```bash
sudo ufw --force enable
```

Флаг `--force` пропускает интерактивный вопрос и сразу включает файрвол.

---

### СПОСОБ 2: Использовать английскую букву через echo

**Выполните:**

```bash
echo y | sudo ufw enable
```

(Важно: используйте английскую букву `y`, а не русскую!)

---

### СПОСОБ 3: Настроить локаль (если предыдущие не помогли)

**Выполните эти команды:**

```bash
export LC_ALL=C
echo "y" | sudo ufw enable
```

---

## ✅ ПРОВЕРКА

После выполнения одного из способов, проверьте статус:

```bash
sudo ufw status
```

**Должно показать:**
```
Status: active
```

И список правил:
```
To                         Action      From
--                         ------      ----
22/tcp                     ALLOW       Anywhere
80/tcp                     ALLOW       Anywhere
443/tcp                    ALLOW       Anywhere
```

---

## 📝 ЧТО ДАЛЬШЕ

Если файрвол успешно включен (`Status: active`), напишите мне:
- ✅ **"Готово, файрвол включен"** - перейдем к следующему шагу

---

**Статус:** 🔧 Исправление проблемы с кодировкой

