# ✅ ПРОВЕРКА: API через домен

## 🎯 ЦЕЛЬ

**Убедиться что бэкенд доступен через `https://ringoouchet.ru/api/`**

---

## ✅ ПРОВЕРКА 1: API через домен

**На сервере:**

```bash
curl -k https://ringoouchet.ru/api/health/
```

**Что должно быть:**
- ✅ JSON: `{"status": "healthy", ...}`
- ❌ Ошибка 404/502 - проблема!

**Ранее вы показали что это работает! ✅**

---

## ✅ ПРОВЕРКА 2: Конфигурация Nginx для API

**На сервере:**

```bash
sudo cat /etc/nginx/sites-available/ringo-uchet | grep -A 10 "location /api/"
```

**Что должно быть:**
```nginx
location /api/ {
    proxy_pass http://127.0.0.1:8001;
    ...
}
```

**Пришлите вывод!**

---

## ✅ ПРОВЕРКА 3: Flutter конфигурация

**В `mobile/lib/core/config/app_config.dart` должно быть:**

```dart
apiBaseUrl: 'https://ringoouchet.ru',
```

**Это значит API URL будет:** `https://ringoouchet.ru/api/v1`

**Проверим файл:**

```bash
# На вашем компьютере
cat mobile/lib/core/config/app_config.dart | grep -A 2 "prod"
```

---

**Выполните проверки 2-3 и пришлите результаты!**

