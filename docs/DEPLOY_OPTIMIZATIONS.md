# 🚀 ДЕПЛОЙ ОПТИМИЗАЦИЙ В ПРОДАКШЕН

## 🎯 ЦЕЛЬ

**Загрузить все оптимизации из OPTIMIZATION_REPORT.md на сервер Beget и активировать их.**

---

## ✅ ШАГ 1: Пересобрать Flutter Web с оптимизациями

**На вашем компьютере (PowerShell):**

```powershell
cd C:\ringo-uchet\mobile

# Если flutter clean не работает (файлы заняты), пропустите его
# flutter clean  # ПРОПУСТИТЕ если ошибка про .dart_tool

flutter pub get
flutter build web --release --base-href /
```

⏱️ **Займет 3-5 минут**

**Важно:** 
- Если `flutter clean` выдает ошибку про `.dart_tool` - **просто пропустите его**
- Это не критично, сборка все равно будет работать
- Убедитесь, что все изменения сохранены в `mobile/`
- Проверьте, что `service-worker.js` есть в `mobile/web/service-worker.js`

### 🔧 Если flutter clean не работает:
- Закройте VS Code / Cursor (если открыт)
- Закройте все терминалы с Flutter процессами
- Или просто **пропустите** `flutter clean` - сборка все равно будет работать!

---

## ✅ ШАГ 2: Проверить наличие service-worker.js

**На вашем компьютере:**

```powershell
cd C:\ringo-uchet\mobile\build\web
Test-Path service-worker.js
```

**Должно вернуть:** `True`

**Если `False`, скопируйте вручную:**

```powershell
Copy-Item C:\ringo-uchet\mobile\web\service-worker.js -Destination C:\ringo-uchet\mobile\build\web\service-worker.js -Force
```

---

## ✅ ШАГ 3: Создать архив

**На вашем компьютере:**

```powershell
cd C:\ringo-uchet\mobile\build
Compress-Archive -Path web\* -DestinationPath web-build-optimized.zip -Force
```

---

## ✅ ШАГ 4: Загрузить на сервер

**На вашем компьютере:**

```powershell
scp C:\ringo-uchet\mobile\build\web-build-optimized.zip root@91.229.90.72:~/web-build-optimized.zip
```

**Введите пароль root когда попросит.**

---

## ✅ ШАГ 5: На сервере - обновить файлы

**Подключитесь к серверу:**

```bash
ssh root@91.229.90.72
```

**На сервере выполните:**

```bash
cd ~

# Создать резервную копию текущей версии (на всякий случай)
sudo mkdir -p /var/www/ringo-uchet-backup
sudo cp -r /var/www/ringo-uchet/* /var/www/ringo-uchet-backup/ 2>/dev/null || true

# Очистить старые файлы
sudo rm -rf /var/www/ringo-uchet/*

# Распаковать новую версию
unzip -o ~/web-build-optimized.zip -d /tmp/flutter-web/

# Переместить файлы
sudo mv /tmp/flutter-web/web/* /var/www/ringo-uchet/ 2>/dev/null || sudo mv /tmp/flutter-web/* /var/www/ringo-uchet/

# Установить правильные права
sudo chown -R www-data:www-data /var/www/ringo-uchet
sudo chmod -R 755 /var/www/ringo-uchet

# Проверить наличие файлов
ls -la /var/www/ringo-uchet/ | head -15
```

---

## ✅ ШАГ 6: Проверить service-worker.js

**На сервере:**

```bash
ls -la /var/www/ringo-uchet/service-worker.js
cat /var/www/ringo-uchet/service-worker.js | head -20
```

**Должен быть файл `service-worker.js` с содержимым.**

**Если файла нет, скопируйте вручную:**

```bash
# Проверить где он в архиве
unzip -l ~/web-build-optimized.zip | grep service-worker

# Если есть, распаковать только его
unzip -j ~/web-build-optimized.zip service-worker.js -d /var/www/ringo-uchet/
sudo chown www-data:www-data /var/www/ringo-uchet/service-worker.js
```

---

## ✅ ШАГ 7: Проверить index.html

**На сервере:**

```bash
grep -A 20 "Service Worker" /var/www/ringo-uchet/index.html
```

**Должна быть секция регистрации Service Worker.**

---

## ✅ ШАГ 8: Перезагрузить Nginx (если нужно)

**На сервере:**

```bash
sudo nginx -t
sudo systemctl reload nginx
```

---

## ✅ ШАГ 9: Проверка в браузере

**На вашем компьютере:**

1. Откройте `https://ringoouchet.ru`
2. Откройте DevTools (F12)
3. Перейдите на вкладку **Application** → **Service Workers**
4. Должен быть зарегистрирован Service Worker
5. Проверьте **Console** - должно быть сообщение: `Service Worker registered`

---

## ✅ ШАГ 10: Очистить кэш браузера

**Важно:** Для тестирования оптимизаций нужно очистить кэш:

### На ПК:
- **Chrome/Edge:** `Ctrl+Shift+Delete` → Очистить данные → Только кэш
- Или `Ctrl+F5` для жесткой перезагрузки

### На телефоне:
1. Откройте настройки браузера
2. Найдите "Данные сайтов" или "Очистить данные"
3. Очистите данные для `ringoouchet.ru`
4. Или откройте сайт в режиме инкогнито

---

## ✅ ШАГ 11: Проверить работу оптимизаций

### ✅ Проверка 1: Service Worker (ОБЯЗАТЕЛЬНО)
**В консоли браузера должно быть:**
```
Service Worker registered: https://ringoouchet.ru/
[Service Worker] Installing...
[Service Worker] Caching static assets
[Service Worker] Activating...
IndexedDB initialized successfully
```

**Ошибка с Notification не критична** - это Firebase Push, приложение работает нормально.

### Проверка 2: Скорость загрузки
- Закройте и откройте сайт снова
- UI должен появиться **<1 секунда** (из кэша)

### Проверка 3: Офлайн режим
1. Откройте сайт
2. В DevTools → **Network** → выберите **Offline**
3. Обновите страницу
4. Приложение должно работать с кэшированными данными

### Проверка 4: Cache Storage
- DevTools → **Application** → **Service Workers** → должен быть активен
- DevTools → **Application** → **Cache Storage** → должны быть кэши:
  - `ringo-static-v1`
  - `ringo-api-v1` (после использования API)

---

## 🔧 РЕШЕНИЕ ПРОБЛЕМ

### Service Worker не регистрируется
```bash
# На сервере - проверить права
sudo ls -la /var/www/ringo-uchet/service-worker.js
sudo chmod 644 /var/www/ringo-uchet/service-worker.js
```

### Файлы не обновляются
```bash
# На сервере - очистить старый кэш Nginx
sudo rm -rf /var/cache/nginx/*
sudo systemctl reload nginx
```

### Ошибки в консоли браузера
- Проверьте `index.html` - должен быть скрипт регистрации Service Worker
- Проверьте `service-worker.js` - должен быть в корне `/var/www/ringo-uchet/`

---

## 📋 КОНТРОЛЬНЫЙ СПИСОК

- [ ] Flutter Web пересобран (`flutter build web`)
- [ ] `service-worker.js` присутствует в `build/web/`
- [ ] Архив создан и загружен на сервер
- [ ] Файлы распакованы в `/var/www/ringo-uchet/`
- [ ] Права установлены (`www-data:www-data`, `755`)
- [ ] `service-worker.js` доступен по пути `/service-worker.js`
- [ ] Nginx перезагружен
- [ ] Service Worker зарегистрирован в браузере
- [ ] Приложение работает быстрее (<1 сек загрузка)

---

**Начните с ШАГА 1 - пересоберите Flutter Web!**

