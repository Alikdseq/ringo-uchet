# 🚀 ПОЛНАЯ ИНСТРУКЦИЯ ПО ДЕПЛОЮ ВСЕХ ИЗМЕНЕНИЙ

## 📋 ЧТО БУДЕТ ЗАДЕПЛОЕНО

### ✅ НОВЫЕ ФУНКЦИИ (Последнее обновление)

#### Backend (Django):
1. **Удаление заявок**
   - Добавлен статус `DELETED` в `OrderStatus`
   - Новый endpoint `/api/v1/orders/{id}/delete/` для "мягкого" удаления
   - Удаленные заявки исключаются из финансовых отчетов
   - Админы могут видеть удаленные заявки

2. **Создание заявок с номенклатурой**
   - Возможность добавлять номенклатуру (техника, услуги, материалы) при создании заявки
   - Поддержка поля `total_amount` для примерной стоимости
   - Возможность сразу одобрить заявку при создании

3. **Редактирование заявок для админа**
   - Админы могут редактировать заявки на любом этапе
   - Возможность изменять номенклатуру, стоимость, операторов

4. **CORS настройки**
   - Добавлены настройки CORS в `prod.py` для разрешения localhost
   - Поддержка регулярных выражений для всех localhost портов

#### Frontend (Flutter):
1. **Удаление заявок**
   - Кнопка "Удалить заявку" для менеджеров/админов
   - Статус "Удалён" отображается в списке заявок
   - Удаленные заявки скрыты для не-админов

2. **Создание заявок**
   - Диалог выбора номенклатуры (техника, услуги, материалы)
   - Поле для указания примерной стоимости
   - Чекбокс "Одобрить заявку при создании"
   - Возможность указать количество и скидку для каждого элемента

3. **Редактирование заявок (ПРЯМО В ДЕТАЛЯХ)**
   - **Все поля редактируемы прямо в деталях заявки для админа** (без отдельного экрана)
   - Адрес, описание, стоимость, номенклатура, операторы - все редактируется на месте
   - Автосохранение стоимости с debounce (5 секунд или при потере фокуса)
   - Предотвращение дублирования операторов
   - Правильное отображение данных на всех этапах (техника, цена, операторы)

4. **Улучшения UI**
   - Описание техники отображается под названием в карточке номенклатуры
   - Кнопка отправки чека в WhatsApp после завершения заявки
   - Улучшенная обработка статусов заявок
   - Убрано поле фото из диалога смены статуса (только комментарий)

5. **Исправления**
   - Исправлена инициализация Flutter Engine для веб (удален устаревший `window.flutterConfiguration`)
   - Исправлены ошибки компиляции с `OrderStatus.deleted`
   - Исправлено поле `totalAmount` в `OrderRequest`
   - Исправлено автосохранение цены (debounce 5 секунд)
   - Исправлено отображение данных на всех этапах

### ✅ Исправления мобильного интернета
- Service Worker v2 с таймаутами и retry
- RetryInterceptor с улучшенной обработкой ошибок
- Улучшенная обработка сетевых ошибок

### ✅ Исправления UI/UX
- Адаптивность чисел в карточке "Доход"
- Навигация через нижнюю панель для кнопок "Новые заявки" и "Назначенные"
- Удобная навигация для быстрых действий (AppBar с кнопкой назад)

### ✅ Оптимизации производительности
- Service Worker для кэширования
- Gzip сжатие
- Оптимизация загрузки
- **HTML renderer вместо CanvasKit (экономия ~20 MB)**
- **Удаление debug символов (экономия ~5.5 MB)**
- **Итоговый размер: ~6-7 MB вместо 31 MB (экономия ~80%)**

---

## ✅ ШАГ 1: Пересобрать Flutter Web с оптимизацией

**На вашем компьютере (PowerShell):**

```powershell
cd C:\ringo-uchet\mobile

# Обновить зависимости (если нужно)
flutter pub get

# Пересобрать для production (HTML renderer указан в index.html)
flutter build web --release --base-href /
```

⏱️ **Займет 3-5 минут**

**Важно:**
- HTML renderer настроен автоматически (удален устаревший `window.flutterConfiguration`)
- Flutter автоматически использует HTML renderer (экономия ~20 MB)
- Если `flutter clean` выдает ошибку про `.dart_tool` - **просто пропустите его**
- Убедитесь, что все изменения сохранены в `mobile/` и `backend/`

---

## ✅ ШАГ 2: Проверить наличие всех файлов

**На вашем компьютере:**

```powershell
cd C:\ringo-uchet\mobile\build\web

# Проверить service-worker.js (должен быть v2)
Test-Path service-worker.js
Get-Content service-worker.js | Select-String -Pattern "v2" | Select-Object -First 1


# Проверить index.html
Test-Path index.html

# Проверить main.dart.js (главный файл приложения)
Test-Path main.dart.js
```

**Все должно вернуть `True` и показать содержимое.**

**Если `service-worker.js` отсутствует или не v2, скопируйте вручную:**

```powershell
Copy-Item C:\ringo-uchet\mobile\web\service-worker.js -Destination C:\ringo-uchet\mobile\build\web\service-worker.js -Force
```

---

## ✅ ШАГ 3: Очистить сборку от ненужных файлов

**На вашем компьютере:**

```powershell
cd C:\ringo-uchet\mobile\build\web

# Удалить .symbols файлы (debug символы, ~5.5 MB экономии)
Get-ChildItem -Recurse -Filter "*.symbols" | Remove-Item -Force

# Удалить NOTICES файлы (лицензии, не нужны в production)
Get-ChildItem -Recurse -Filter "NOTICES" | Remove-Item -Force

# Проверить итоговый размер (должен быть ~6-7 MB вместо 31 MB)
Get-ChildItem -Recurse -File | Measure-Object -Property Length -Sum | Select-Object @{Name="TotalSize(MB)";Expression={[math]::Round($_.Sum/1MB,2)}}
```

**Или использовать скрипт:**

```powershell
cd C:\ringo-uchet\mobile
.\scripts\clean_build.ps1
```

---

## ✅ ШАГ 4: Создать архив

**На вашем компьютере:**

```powershell
cd C:\ringo-uchet\mobile\build

# Создать архив со всеми файлами из web/
Compress-Archive -Path web\* -DestinationPath web-build-all-changes.zip -Force

# Проверить размер архива (должен быть ~6-7 MB после оптимизации)
Get-Item web-build-all-changes.zip | Select-Object Name, @{Name="Size(MB)";Expression={[math]::Round($_.Length/1MB,2)}}
```

**Ожидаемый размер:** ~6-7 MB (вместо 11 MB)

---

## ✅ ШАГ 5: Загрузить архив на сервер

**Вариант A: Через scp (если есть доступ)**

```powershell
# На вашем компьютере
scp C:\ringo-uchet\mobile\build\web-build-all-changes.zip root@91.229.90.72:~/web-build-all-changes.zip
```

**Введите пароль root когда попросит.**

**Вариант B: Через FTP/SFTP клиент (FileZilla, WinSCP и т.д.)**
- Подключитесь к серверу `91.229.90.72`
- Загрузите `web-build-all-changes.zip` в домашнюю директорию `/root/`

---

## ✅ ШАГ 6: Подключиться к серверу

**На вашем компьютере:**

```powershell
ssh root@91.229.90.72
```

**Введите пароль root когда попросит.**

---

## ✅ ШАГ 7: На сервере - создать резервную копию

**На сервере:**

```bash
# Создать резервную копию текущей версии (на всякий случай)
sudo mkdir -p /var/www/ringo-uchet-backup-$(date +%Y%m%d-%H%M%S)
sudo cp -r /var/www/ringo-uchet/* /var/www/ringo-uchet-backup-$(date +%Y%m%d-%H%M%S)/ 2>/dev/null || true

echo "✅ Резервная копия создана"
```

---

## ✅ ШАГ 8: Распаковать новую версию

**На сервере:**

```bash
cd ~

# Проверить что архив загружен
ls -lh web-build-all-changes.zip

# Распаковать во временную директорию
unzip -o web-build-all-changes.zip -d /tmp/flutter-web-new/

# Проверить что файлы распакованы
ls -la /tmp/flutter-web-new/ | head -20
```

---

## ✅ ШАГ 9: Обновить файлы на сервере

**На сервере:**

```bash
# Очистить старые файлы (кроме резервной копии)
sudo rm -rf /var/www/ringo-uchet/*

# Переместить новые файлы
sudo mv /tmp/flutter-web-new/web/* /var/www/ringo-uchet/ 2>/dev/null || sudo mv /tmp/flutter-web-new/* /var/www/ringo-uchet/

# Установить правильные права
sudo chown -R www-data:www-data /var/www/ringo-uchet
sudo chmod -R 755 /var/www/ringo-uchet

# Проверить что файлы на месте
ls -la /var/www/ringo-uchet/ | head -20
```

---

## ✅ ШАГ 10: Проверить service-worker.js

**На сервере:**

```bash
# Проверить наличие файла
ls -la /var/www/ringo-uchet/service-worker.js

# Проверить версию (должно быть v2)
head -5 /var/www/ringo-uchet/service-worker.js | grep -i "v2\|CACHE_NAME"

# Проверить права доступа
sudo chmod 644 /var/www/ringo-uchet/service-worker.js
sudo chown www-data:www-data /var/www/ringo-uchet/service-worker.js
```

**Должно показать:**
- Файл существует
- Версия v2 в коде
- Права: `-rw-r--r-- www-data www-data`

---

## ✅ ШАГ 11: Проверить index.html

**На сервере:**

```bash
# Проверить регистрацию Service Worker
grep -A 10 "Service Worker" /var/www/ringo-uchet/index.html

# Проверить наличие main.dart.js
ls -lh /var/www/ringo-uchet/main.dart.js
```

**Должна быть секция регистрации Service Worker и файл main.dart.js.**

---

## ✅ ШАГ 12: Перезагрузить Nginx

**На сервере:**

```bash
# Проверить конфигурацию Nginx
sudo nginx -t

# Если все ОК, перезагрузить
sudo systemctl reload nginx

echo "✅ Nginx перезагружен"
```

---

## ✅ ШАГ 13: Очистить кэш Nginx (опционально)

**На сервере:**

```bash
# Очистить кэш Nginx для гарантии свежих файлов
sudo rm -rf /var/cache/nginx/*
sudo systemctl reload nginx

echo "✅ Кэш Nginx очищен"
```

---

## ✅ ШАГ 14: Проверка в браузере

**На вашем компьютере:**

### 1. Откройте сайт
- Откройте `https://ringoouchet.ru`
- Откройте DevTools (`F12`)

### 2. Проверьте Service Worker
- Перейдите на вкладку **Application** → **Service Workers**
- Должен быть зарегистрирован Service Worker **v2**
- Статус: **activated and running**

### 3. Проверьте Console
- Перейдите на вкладку **Console**
- Должно быть сообщение: `Service Worker registered: https://ringoouchet.ru/`
- Должны быть логи: `[Service Worker] Installing v2...`, `[Service Worker] Activating v2...`

### 4. Проверьте Network
- Перейдите на вкладку **Network**
- Обновите страницу (`Ctrl+R` или `F5`)
- Проверьте что файлы загружаются
- Проверьте что `service-worker.js` загружается с кодом 200

---

## ✅ ШАГ 15: Обновить Backend (если нужно)

## ✅ ШАГ 15: Обновить Backend (Django)

**На сервере:**

```bash
cd ~/ringo-uchet/backend

# 1. Убедитесь что контейнеры запущены
docker compose -f docker-compose.prod.yml ps

# 2. Создать резервную копию базы данных (ВАЖНО: база называется ringo_prod!)
docker compose -f docker-compose.prod.yml exec db pg_dump -U ringo_user ringo_prod > /root/backup-$(date +%Y%m%d-%H%M%S).sql

# 3. Проверить что бэкап создан (должен быть больше 0 байт)
ls -lh /root/backup-*.sql | tail -1

# 4. Обновить код (ветка master, не main!)
git pull origin master

# 5. Перезапустить API контейнер для применения изменений
docker compose -f docker-compose.prod.yml restart api

# 6. Проверить что контейнер перезапустился
docker compose -f docker-compose.prod.yml ps api

# 7. Проверить логи на ошибки
docker compose -f docker-compose.prod.yml logs api --tail 50

# 8. Проверить что endpoint удаления зарегистрирован (должен быть в списке)
docker compose -f docker-compose.prod.yml exec api python manage.py show_urls | grep delete
# Или проверьте в коде:
# grep -r "delete_order" backend/orders/views.py
# Должна быть строка: @action(detail=True, methods=["post"], url_path="delete")

# 9. Если endpoint не работает, проверьте что код обновлен:
docker compose -f docker-compose.prod.yml exec api grep -n "delete_order" /app/orders/views.py
# Должна быть строка с @action(detail=True, methods=["post"], url_path="delete")
```

**Важно:** 
- База данных называется `ringo_prod`, а не `ringo_db`
- Ветка в git называется `master`, а не `main`
- После перезапуска API endpoint `/api/v1/orders/{id}/delete/` должен работать
- Убедитесь, что переменная окружения `CORS_ALLOWED_ORIGINS` не установлена (или пуста) для использования регулярных выражений для localhost

---

## ✅ ШАГ 16: Очистить кэш браузера

**ВАЖНО:** Для тестирования новых изменений нужно очистить кэш:

### На ПК (Chrome/Edge):
1. `F12` → **Application** → **Service Workers**
2. Нажмите **Unregister** для старого Service Worker (если есть)
3. `F12` → **Application** → **Clear storage**
4. Нажмите **Clear site data**
5. Или используйте `Ctrl+Shift+Delete` → Очистить данные → Только кэш

### На телефоне:
1. **Chrome/Edge:**
   - Настройки → Конфиденциальность → Очистить данные
   - Выберите "Кэш" и "Сервисные воркеры"
   - Очистите данные для `ringoouchet.ru`

2. **Safari (iOS):**
   - Настройки → Safari → Очистить историю и данные
   - Или: Долгое нажатие на кнопку обновления → Очистить кэш

3. **Yandex Browser:**
   - Настройки → Конфиденциальность → Очистить данные
   - Выберите "Кэш" и "Сервисные воркеры"

---

## ✅ ШАГ 17: Проверка всех исправлений

### ✅ Проверка 1: Service Worker v2
**В консоли браузера должно быть:**
```
Service Worker registered: https://ringoouchet.ru/
[Service Worker] Installing v2...
[Service Worker] Caching static assets
[Service Worker] Activating v2...
```

**В Application → Service Workers:**
- Должен быть активен Service Worker
- Scope: `https://ringoouchet.ru/`
- Версия: v2 (можно проверить в коде)

### ✅ Проверка 2: Адаптивность чисел
1. Откройте главный экран (Dashboard)
2. Проверьте карточку "Доход"
3. Большие числа (например, 46000 ₽) должны корректно отображаться и не выходить за экран
4. Попробуйте на мобильном устройстве - числа должны адаптироваться

### ✅ Проверка 3: Навигация "Новые заявки" и "Назначенные"
1. На главном экране нажмите на карточку "Новые заявки"
2. **Должно:** Переключиться на экран заявок с **нижней панелью навигации**
3. **Не должно:** Открываться отдельный экран без нижней панели
4. Проверьте кнопку "Назначенные" - должно работать так же

### ✅ Проверка 4: Навигация быстрых действий
1. На главном экране нажмите "Создать заявку" в разделе "Быстрые действия"
2. **Должно:** Открыться экран создания заявки с **AppBar и кнопкой "Назад"**
3. Нажмите "Назад" - должно вернуться на главный экран
4. Проверьте кнопку "Отчёты" (для админа) - должно переключать через нижнюю панель

### ✅ Проверка 5: Работа на мобильном интернете
1. Отключите Wi-Fi на телефоне
2. Включите мобильный интернет
3. Откройте `https://ringoouchet.ru`
4. Попробуйте войти в личный кабинет
5. **Должно:** Работать без ошибок, логи должны идти на сервер

**Проверьте логи на сервере:**
```bash
cd /root/ringo-uchet/backend
docker compose -f docker-compose.prod.yml logs -f api
```

**Должны появиться логи:**
- `Token obtain attempt` при попытке входа
- Логи нормализации телефона
- Логи поиска пользователя

### ✅ Проверка 6: Скорость загрузки
1. Закройте и откройте сайт снова
2. UI должен появиться **<1 секунда** (из кэша Service Worker)
3. Проверьте в Lighthouse (F12 → Lighthouse → Performance)
4. Должна быть хорошая оценка производительности

### ✅ Проверка 7: Новые функции заявок

#### 7.1 Создание заявки с номенклатурой
1. Откройте список заявок
2. Нажмите "Создать заявку"
3. Заполните данные клиента и адрес
4. Нажмите "Добавить номенклатуру"
5. **Должно:** Открыться диалог с выбором техники/услуг/материалов
6. Добавьте несколько позиций с количеством и скидкой
7. Укажите примерную стоимость (например, 70000)
8. Поставьте галочку "Одобрить заявку при создании" (опционально)
9. Создайте заявку
10. **Должно:** Заявка создана с указанной стоимостью, номенклатурой и статусом (создан или одобрен)

#### 7.2 Редактирование заявки админом (прямо в деталях)
1. Откройте детали заявки (войдите как админ)
2. **Должно:** Все поля редактируемы прямо в деталях:
   - Адрес - текстовое поле
   - Описание - текстовое поле
   - Стоимость - числовое поле с автосохранением
   - Номенклатура - кнопка "Добавить" и кнопки удаления для каждого элемента
   - Операторы - чекбоксы для выбора
3. Измените стоимость (например, введите 75000)
4. **Должно:** Сохранение происходит через 5 секунд после последнего изменения ИЛИ при потере фокуса
5. Добавьте оператора
6. **Должно:** Если оператор уже добавлен, показывается предупреждение "Оператор уже добавлен"
7. Удалите элемент номенклатуры
8. **Должно:** Элемент удаляется и изменения сохраняются

#### 7.3 Отображение данных на всех этапах
1. Создайте заявку с техникой, ценой 70000 и оператором
2. На этапе "Создан" или "Одобрен" откройте детали заявки
3. **Должно:** Видны все данные:
   - Техника в разделе "Номенклатура"
   - Стоимость 70000 ₽ в разделе "Финансы"
   - Оператор в разделе "Операторы"
4. **НЕ должно:** Показываться 0.00 ₽ если была указана стоимость

#### 7.4 Удаление заявки
1. Откройте детали заявки (войдите как админ или менеджер)
2. Нажмите кнопку "Удалить заявку"
3. Подтвердите удаление
4. **Должно:** Заявка переходит в статус "Удалён"
5. **Должно:** Заявка исключена из отчетов
6. **Должно:** Не-админы не видят удаленные заявки в списке

#### 7.5 Отправка чека в WhatsApp
1. Завершите заявку (статус "Завершён")
2. Нажмите "Скачать чек"
3. После скачивания должно появиться диалоговое окно
4. **Должно:** Быть предложение отправить чек в WhatsApp (если у клиента есть телефон)
5. Нажмите "Отправить в WhatsApp"
6. **Должно:** Открыться WhatsApp с номером клиента

#### 7.6 Диалог смены статуса
1. Откройте детали заявки
2. Нажмите кнопку смены статуса (например, "Одобрить")
3. **Должно:** Открыться диалог с полем "Комментарий"
4. **НЕ должно:** Быть поля для фото (убрано)
5. Введите комментарий и нажмите "Изменить"
6. **Должно:** Статус изменился с комментарием

#### 7.7 Описание техники
1. Откройте каталог номенклатуры
2. Перейдите на вкладку "Техника"
3. **Должно:** Описание отображается под названием техники
4. **Должно:** Код и цена в subtitle

#### 7.8 Отправка чека в WhatsApp
1. Завершите заявку (статус "Завершён")
2. Нажмите "Получить чек"
   - После загрузки чека нажмите "Отправить в WhatsApp"
   - Проверьте что WhatsApp открывается с правильным номером клиента

---

## 🔧 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: Service Worker не регистрируется

**Решение:**
```bash
# На сервере - проверить права
sudo ls -la /var/www/ringo-uchet/service-worker.js
sudo chmod 644 /var/www/ringo-uchet/service-worker.js
sudo chown www-data:www-data /var/www/ringo-uchet/service-worker.js

# Проверить что файл доступен по HTTPS
curl -I https://ringoouchet.ru/service-worker.js
```

**Должен вернуть:** `200 OK`

### Проблема: Файлы не обновляются

**Решение:**
```bash
# На сервере - очистить кэш Nginx
sudo rm -rf /var/cache/nginx/*
sudo systemctl reload nginx

# Очистить кэш браузера (см. ШАГ 14)
```

### Проблема: Ошибки в консоли браузера

**Решение:**
1. Проверьте что `service-worker.js` версии v2 на сервере
2. Проверьте что `index.html` содержит скрипт регистрации Service Worker
3. Очистите кэш браузера полностью
4. Проверьте Network tab - все файлы должны загружаться с кодом 200

### Проблема: Навигация не работает через нижнюю панель

**Решение:**
1. Проверьте что `navigation_provider.dart` загружен в `main.dart.js`
2. Очистите кэш браузера
3. Перезагрузите страницу (`Ctrl+Shift+R` или `Ctrl+F5`)

### Проблема: Числа все еще выходят за экран

**Решение:**
1. Убедитесь что обновлен `dashboard_screen.dart` в сборке
2. Очистите кэш браузера
3. Проверьте на реальном мобильном устройстве (не только в DevTools)

---

## 📋 КОНТРОЛЬНЫЙ СПИСОК

**Перед деплоем:**
- [ ] Flutter Web пересобран с HTML renderer (`flutter build web --release --base-href /`)
- [ ] Сборка очищена от .symbols файлов (экономия ~5.5 MB)
- [ ] Размер сборки ~6-7 MB (вместо 31 MB)
- [ ] `service-worker.js` присутствует в `build/web/` и версия v2
- [ ] `index.html` присутствует в `build/web/` (без устаревшего `window.flutterConfiguration`)
- [ ] `main.dart.js` присутствует в `build/web/`
- [ ] Архив создан (`web-build-all-changes.zip`, размер ~6-7 MB)
- [ ] Backend изменения применены (если деплоите изменения бэкенда)
- [ ] Миграции применены (если есть новые)

**На сервере:**
- [ ] Архив загружен на сервер
- [ ] Резервная копия создана
- [ ] Файлы распакованы в `/var/www/ringo-uchet/`
- [ ] Права установлены (`www-data:www-data`, `755`)
- [ ] `service-worker.js` доступен по пути `/service-worker.js`
- [ ] Nginx перезагружен

**После деплоя:**
- [ ] Service Worker v2 зарегистрирован в браузере
- [ ] Адаптивность чисел работает корректно
- [ ] Навигация через нижнюю панель работает
- [ ] Быстрые действия имеют AppBar с кнопкой назад
- [ ] Работает на мобильном интернете
- [ ] Логи идут на сервер с мобильного интернета
- [ ] Приложение работает быстро (<1 сек загрузка)
- [ ] Создание заявок с номенклатурой работает
- [ ] Примерная стоимость сохраняется при создании
- [ ] Одобрение при создании работает
- [ ] Редактирование заявок доступно для админа
- [ ] Удаление заявок работает корректно
- [ ] Описание техники отображается в карточке
- [ ] Отправка чека в WhatsApp работает
- [ ] CORS ошибки исправлены

---

## 🎯 БЫСТРАЯ СПРАВКА

**Основные команды:**

```powershell
# Пересобрать Flutter Web с оптимизацией
cd C:\ringo-uchet\mobile
flutter build web --release --base-href /

# Очистить от ненужных файлов
cd build\web
Get-ChildItem -Recurse -Filter "*.symbols" | Remove-Item -Force
Get-ChildItem -Recurse -Filter "NOTICES" | Remove-Item -Force

# Создать архив
cd ..
Compress-Archive -Path web\* -DestinationPath web-build-all-changes.zip -Force

# Загрузить на сервер
scp web-build-all-changes.zip root@91.229.90.72:~/

# На сервере - обновить файлы
ssh root@91.229.90.72
sudo rm -rf /var/www/ringo-uchet/*
unzip -o ~/web-build-all-changes.zip -d /tmp/flutter-web/
sudo mv /tmp/flutter-web/web/* /var/www/ringo-uchet/ 2>/dev/null || sudo mv /tmp/flutter-web/* /var/www/ringo-uchet/
sudo chown -R www-data:www-data /var/www/ringo-uchet
sudo chmod -R 755 /var/www/ringo-uchet
sudo systemctl reload nginx
```

---

**Начните с ШАГА 1 - пересоберите Flutter Web!**

**После выполнения всех шагов все изменения будут задеплоены и протестированы!**

