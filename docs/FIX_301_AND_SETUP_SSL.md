# ✅ ИСПРАВЛЕНИЕ: 301 редирект и настройка SSL

## ❌ ПРОБЛЕМА

**Nginx/Django редиректит на HTTPS:**
```
HTTP/1.1 301 Moved Permanently
Location: https://ringoouchet.ru/api/health/
```

**Но SSL еще не настроен!**

---

## ✅ РЕШЕНИЕ

**Есть два пути:**

### Вариант А: Временно отключить редирект, проверить работу, потом настроить SSL

### Вариант Б: Сразу настроить SSL (рекомендуется)

**Я рекомендую вариант Б - настроим SSL прямо сейчас!**

---

## 🚀 НАСТРОЙКА SSL (ПОЛНОЕ РЕШЕНИЕ)

### ШАГ 1: Проверить конфигурацию Nginx

```bash
sudo cat /etc/nginx/sites-available/ringo-uchet
```

**Посмотрим, есть ли там редирект на HTTPS.**

---

### ШАГ 2: Обновить Nginx для SSL (подготовка)

```bash
sudo nano /etc/nginx/sites-available/ringo-uchet
```

**Нужно настроить для получения SSL сертификата.**

---

### ШАГ 3: Создать директорию для Certbot

```bash
sudo mkdir -p /var/www/certbot
sudo chmod -R 755 /var/www/certbot
```

---

### ШАГ 4: Получить SSL сертификат через Certbot

```bash
sudo certbot certonly --webroot -w /var/www/certbot -d ringoouchet.ru -d www.ringoouchet.ru
```

---

**Готовы начать настройку SSL? Или сначала отключим редирект и проверим работу?**

