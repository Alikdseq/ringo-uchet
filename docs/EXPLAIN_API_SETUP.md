# 📋 ОБЪЯСНЕНИЕ: Как работает API через домен

## ✅ КАК ЭТО РАБОТАЕТ

### 1. **Flutter Web** работает на:
- `https://ringoouchet.ru` (порт 443)

### 2. **API запросы** идут на:
- `https://ringoouchet.ru/api/v1/...`

### 3. **Nginx** проксирует `/api/` на:
- `http://localhost:8001` (Django контейнер)

### 4. **Django** работает на:
- `http://localhost:8001` (внутри сервера)
- Доступен через Nginx прокси на домене

---

## ✅ ВСЁ УЖЕ РАБОТАЕТ!

**Мы ранее проверили:**
```bash
curl -k https://ringoouchet.ru/api/health/
# Вернул: {"status": "healthy", ...}
```

**Значит:**
- ✅ API доступен через домен
- ✅ Nginx правильно проксирует
- ✅ Django работает

---

## 🔧 ПРОБЛЕМА

**Проблема в том, что:**
- Flutter Web использовал `AppConfig.dev` (localhost:8001)
- Мы исправили на `AppConfig.prod` (`https://ringoouchet.ru`)
- Нужно пересобрать и загрузить новую версию!

---

## ✅ ЧТО ДЕЛАТЬ

1. ✅ Файл исправлен (`main_web.dart` использует PROD)
2. ⏳ **Нужно пересобрать Flutter Web**
3. ⏳ **Загрузить на сервер**
4. ⏳ **Очистить кеш на телефоне**

---

**Переходим к пересборке!**

