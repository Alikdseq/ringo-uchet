# 🔍 ПРОВЕРКА: Доступен ли API через домен?

## ❓ ВОПРОС

**Доступен ли бэкенд через `https://ringoouchet.ru/api/`?**

---

## ✅ БЫСТРАЯ ПРОВЕРКА

### ПРОВЕРКА 1: API через домен

**На сервере:**

```bash
curl -k https://ringoouchet.ru/api/health/
```

**Что должно быть:**
- ✅ JSON: `{"status": "healthy", ...}`
- ❌ Ошибка 404/502 - проблема!

**Пришлите результат!**

---

### ПРОВЕРКА 2: Конфигурация Nginx для API

**На сервере:**

```bash
sudo cat /etc/nginx/sites-available/ringo-uchet | grep -A 10 "location /api/"
```

**Что должно быть:**
```nginx
location /api/ {
    proxy_pass http://127.0.0.1:8001;
    ...
}
```

**Пришлите вывод!**

---

### ПРОВЕРКА 3: API локально

**На сервере:**

```bash
curl http://localhost:8001/api/health/
```

**Должен вернуть JSON!**

---

**Выполните все 3 проверки и пришлите результаты!**

