# 🔧 ИСПРАВЛЕНИЕ: Пароль базы данных

## ✅ НАЙДЕНО

- **Пароль в контейнере БД:** `d44b63fbd381ec5d8c`
- **Пользователь:** `ringo_user` (не postgres!)
- **Нужно проверить пароль в .env и исправить**

---

## ✅ ШАГ 1: Проверить пароль в .env

**На сервере:**

```bash
cd /root/ringo-uchet/backend
grep "^DB_PASSWORD=" .env
grep "^POSTGRES_PASSWORD=" .env
```

**Пришлите вывод (или скажите совпадают ли пароли с `d44b63fbd381ec5d8c`)**

---

## ✅ ШАГ 2: Проверить подключение с правильным пользователем

**На сервере:**

```bash
docker exec backend-db-1 psql -U ringo_user -d ringo_prod -c "\conninfo"
```

**Если ошибка - пароль не совпадает!**

---

## 🔧 РЕШЕНИЕ: Обновить пароль в .env ИЛИ в БД

### ВАРИАНТ A: Если пароль в .env другой - обновить его в БД

**На сервере:**

```bash
cd /root/ringo-uchet/backend

# Получить пароль из .env
NEW_PASSWORD=$(grep "^DB_PASSWORD=" .env | cut -d '=' -f2)

# Подключиться к БД и обновить пароль
docker exec backend-db-1 psql -U ringo_user -d ringo_prod -c "ALTER USER ringo_user WITH PASSWORD '${NEW_PASSWORD}';"
```

---

### ВАРИАНТ B: Если пароль в .env правильный - обновить его в БД на текущий из контейнера

**На сервере:**

```bash
cd /root/ringo-uchet/backend

# Установить пароль который сейчас в контейнере
CURRENT_PASSWORD="d44b63fbd381ec5d8c"

# Обновить пароль в БД (используя временный доступ)
docker exec backend-db-1 psql -U ringo_user -d ringo_prod -c "ALTER USER ringo_user WITH PASSWORD '${CURRENT_PASSWORD}';"

# Обновить .env чтобы совпадал
sed -i "s/^DB_PASSWORD=.*/DB_PASSWORD=${CURRENT_PASSWORD}/" .env
sed -i "s/^POSTGRES_PASSWORD=.*/POSTGRES_PASSWORD=${CURRENT_PASSWORD}/" .env
```

---

### ВАРИАНТ C: Пересоздать пользователя с правильным паролем

**Но это сложнее, сначала попробуем варианты A или B!**

---

**Сначала выполните ШАГ 1 и скажите какой пароль в .env!**

