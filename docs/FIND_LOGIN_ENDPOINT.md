# 🔍 ПОИСК ENDPOINT ДЛЯ ВХОДА

## 🔍 ПРОВЕРКА: Какой endpoint использует Flutter?

**В API есть endpoint:**
- `/api/token/` - для получения JWT токена (вход)

**Проверим, какой endpoint использует Flutter приложение.**

---

## 📋 ШАГ 1: Проверить логи Nginx (Access)

```bash
sudo tail -200 /var/log/nginx/ringo-uchet-access.log | grep -i "POST"
```

**Ищите POST запросы - увидите какой endpoint используется!**

---

## 📋 ШАГ 2: Проверить логи Nginx (Error)

```bash
sudo tail -100 /var/log/nginx/ringo-uchet-error.log
```

**Ищите ошибки при POST запросах.**

---

## 📋 ШАГ 3: Проверить endpoint входа

**API использует `/api/token/` для входа. Проверим:**

```bash
curl -X POST https://ringoouchet.ru/api/token/ \
  -H "Content-Type: application/json" \
  -H "Origin: https://ringoouchet.ru" \
  -d '{"phone": "test", "password": "test"}' -v
```

**Что возвращает?**

---

**Выполните шаги 1-2 и отправьте логи - найдем проблему!**

