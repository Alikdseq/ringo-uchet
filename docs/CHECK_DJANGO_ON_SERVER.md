# 🔍 ПРОВЕРКА: Работает ли Django на сервере?

## 🎯 ЦЕЛЬ

**Проверить запущен ли Django бэкенд на сервере Beget!**

---

## ✅ ПРОВЕРКА 1: Запущены ли контейнеры Docker?

### ШАГ 1: Проверить статус контейнеров

```bash
cd /root/ringo-uchet/backend
docker compose -f docker-compose.prod.yml ps
```

**Что искать:**
- ✅ Должен быть контейнер `api` в статусе `Up`
- ✅ Должен быть контейнер `db` (PostgreSQL)
- ✅ Должен быть контейнер `redis`
- ✅ Должен быть контейнер `minio`

**Пришлите вывод команды!**

---

## ✅ ПРОВЕРКА 2: Запущен ли API локально на сервере?

### ШАГ 1: Проверить API через localhost

```bash
curl http://localhost:8001/api/health/
```

**Что искать:**
- ✅ Должен вернуть JSON: `{"status":"ok",...}`
- ❌ Если ошибка - API не работает

**Пришлите результат!**

---

## ✅ ПРОВЕРКА 3: Проверить логи API

### ШАГ 1: Посмотреть последние логи

```bash
cd /root/ringo-uchet/backend
docker compose -f docker-compose.prod.yml logs api --tail=30
```

**Ищите ошибки!**

**Пришлите вывод!**

---

## ✅ ПРОВЕРКА 4: Доступен ли API через домен?

### ШАГ 1: Проверить API через домен

```bash
curl -k https://ringoouchet.ru/api/health/
```

**Что искать:**
- ✅ Должен вернуть JSON
- ❌ Если ошибка - проблема с Nginx или API не запущен

**Пришлите результат!**

---

## 🔧 РЕШЕНИЕ: Если контейнеры не запущены

### ШАГ 1: Перейти в директорию backend

```bash
cd /root/ringo-uchet/backend
```

---

### ШАГ 2: Запустить контейнеры

```bash
docker compose -f docker-compose.prod.yml up -d
```

⏱️ **Займет 1-2 минуты**

---

### ШАГ 3: Проверить что запустились

```bash
docker compose -f docker-compose.prod.yml ps
```

**Все контейнеры должны быть `Up`!**

---

### ШАГ 4: Проверить логи

```bash
docker compose -f docker-compose.prod.yml logs api --tail=50
```

**Ищите ошибки!**

---

**Выполните ПРОВЕРКИ 1-4 и пришлите результаты!**

