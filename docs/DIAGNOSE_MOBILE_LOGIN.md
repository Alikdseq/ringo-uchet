# 🔍 ДИАГНОСТИКА: Ошибка при входе с мобильного

## ❌ ПРОБЛЕМА

- На ноутбуке работает ✅
- На телефоне открывается ✅
- При входе (номер + пароль) - **ошибка сервера** ❌

---

## 🔍 ПРОВЕРКА 1: Логи API (самое важное!)

**На сервере выполните:**

```bash
docker compose -f ~/ringo-uchet/backend/docker-compose.prod.yml logs api | tail -100
```

**Ищите ошибки при попытке входа!**

**Скопируйте последние строки с ошибками.**

---

## 🔍 ПРОВЕРКА 2: Логи Nginx

```bash
sudo tail -50 /var/log/nginx/ringo-uchet-error.log
```

**Ищите ошибки 500 или другие.**

---

## 🔍 ПРОВЕРКА 3: CORS настройки

**Проблема:** CORS может не разрешать HTTPS origins!

**Проверьте:**

```bash
cd ~/ringo-uchet/backend
cat .env | grep CORS
docker compose -f docker-compose.prod.yml exec api env | grep CORS
```

---

## 🔍 ПРОВЕРКА 4: Прямой запрос к API

**Проверьте endpoint входа напрямую:**

```bash
curl -X POST https://ringoouchet.ru/api/auth/login/ \
  -H "Content-Type: application/json" \
  -d '{"phone": "test", "password": "test"}'
```

**Что возвращает?**

---

## ✅ ВОЗМОЖНЫЕ ПРИЧИНЫ

1. **CORS не разрешает HTTPS origins** - нужно обновить CORS_ALLOWED_ORIGINS
2. **Ошибка базы данных** - была ошибка аутентификации БД
3. **Ошибка API** - нужно посмотреть логи

---

**ВЫПОЛНИТЕ ПРОВЕРКУ 1 (логи API) - это самое важное!**

**Пришлите логи, и я найду проблему!**

