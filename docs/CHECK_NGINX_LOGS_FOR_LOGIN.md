# 🔍 ПРОВЕРКА: Логи Nginx при попытке входа

## ❌ ПРОБЛЕМА

**В логах API нет ошибок при входе - возможно проблема в Nginx!**

---

## 🔍 ПРОВЕРКА 1: Логи Nginx Error

```bash
sudo tail -100 /var/log/nginx/ringo-uchet-error.log
```

**Ищите ошибки при попытке входа (POST запросы).**

---

## 🔍 ПРОВЕРКА 2: Логи Nginx Access

```bash
sudo tail -100 /var/log/nginx/ringo-uchet-access.log | grep -i "login\|auth\|POST"
```

**Ищите POST запросы к /api/auth/...**

---

## 🔍 ПРОВЕРКА 3: Мониторинг логов в реальном времени

**Откройте новый терминал и запустите:**

```bash
sudo tail -f /var/log/nginx/ringo-uchet-error.log
```

**И в другом терминале:**

```bash
docker compose -f ~/ringo-uchet/backend/docker-compose.prod.yml logs -f api
```

**Затем попробуйте войти с телефона - увидите ошибки в реальном времени!**

---

## 🔍 ПРОВЕРКА 4: Проверить endpoint входа

**Какой endpoint используется для входа?**

**На сервере проверьте:**

```bash
curl -X POST https://ringoouchet.ru/api/auth/login/ \
  -H "Content-Type: application/json" \
  -d '{"phone": "test", "password": "test"}' -v
```

**Что возвращает?**

---

**Выполните проверки 1-2 и отправьте логи!**

