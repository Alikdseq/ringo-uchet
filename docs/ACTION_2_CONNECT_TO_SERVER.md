# ✅ ДЕЙСТВИЕ 2: ПОДКЛЮЧЕНИЕ К СЕРВЕРУ И ПЕРВИЧНАЯ НАСТРОЙКА

## 🎯 ЦЕЛЬ
Подключиться к серверу по SSH и выполнить первичную настройку системы.

---

## 📋 ЧАСТЬ 1: ПОЛУЧЕНИЕ ДАННЫХ ДЛЯ ПОДКЛЮЧЕНИЯ

### ШАГ 1: Получить IP-адрес сервера

1. **Откройте панель Beget:**
   - Перейдите на https://beget.com
   - Войдите в аккаунт

2. **Найдите ваш сервер:**
   - Откройте раздел **"VPS"** или **"Серверы"**
   - Выберите ваш сервер (Ubuntu 24.04, 3 CPU, 6 GB RAM)

3. **Скопируйте IP-адрес:**
   - IP-адрес обычно показан на странице сервера
   - Может быть в формате: `123.45.67.89` (4 числа через точку)
   - **СКОПИРУЙТЕ ЕГО** - он понадобится дальше

---

### ШАГ 2: Найти пароль root или SSH ключ

**Вариант 1: Пароль root**
- Ищите на странице сервера раздел: **"Пароли"**, **"Доступ"**, **"SSH"**, **"Root пароль"**
- Если пароль не показан, может быть кнопка **"Показать пароль"** или **"Сбросить пароль"**

**Вариант 2: SSH ключ**
- Если вы настроили SSH ключ при создании сервера, используйте его

**Запишите:**
- IP-адрес: `_________________`
- Пароль root: `_________________` (или укажите, что используете SSH ключ)

---

## 📋 ЧАСТЬ 2: ПОДКЛЮЧЕНИЕ К СЕРВЕРУ (Windows)

### ВАРИАНТ А: Использовать встроенный SSH в Windows (PowerShell)

**Это самый простой способ для Windows 10/11:**

1. **Откройте PowerShell:**
   - Нажмите `Windows + R`
   - Введите: `powershell`
   - Нажмите `Enter`

2. **Подключитесь к серверу:**
   ```powershell
   ssh root@ВАШ_IP_АДРЕС
   ```
   
   **Замените `ВАШ_IP_АДРЕС` на реальный IP!**
   
   Например, если IP `123.45.67.89`, то команда будет:
   ```powershell
   ssh root@123.45.67.89
   ```

3. **При первом подключении:**
   - Система спросит: "Are you sure you want to continue connecting (yes/no)?"
   - Напишите: `yes` и нажмите `Enter`

4. **Введите пароль:**
   - Система попросит: "root@ВАШ_IP_АДРЕС's password:"
   - Введите пароль root (пароль не будет виден при вводе - это нормально!)
   - Нажмите `Enter`

5. **Если все правильно:**
   - Вы увидите приглашение: `root@ваш-сервер:~#`
   - **Это значит, вы подключились! ✅**

---

### ВАРИАНТ Б: Использовать PuTTY (если SSH не работает)

**Если встроенный SSH не работает, используйте PuTTY:**

1. **Скачайте PuTTY:**
   - Перейдите: https://www.putty.org/
   - Скачайте и установите PuTTY

2. **Запустите PuTTY:**
   - В поле **"Host Name"** введите: `root@ВАШ_IP_АДРЕС`
   - Или просто IP-адрес, порт `22`
   - Нажмите **"Open"**

3. **При первом подключении:**
   - Появится окно с предупреждением - нажмите **"Да"**

4. **Введите пароль:**
   - Введите пароль root и нажмите `Enter`

---

## 📋 ЧАСТЬ 3: ПЕРВИЧНАЯ НАСТРОЙКА СЕРВЕРА

**После успешного подключения выполните эти команды:**

### ШАГ 1: Обновление системы

Скопируйте и вставьте команду (правой кнопкой мыши в терминал):

```bash
sudo apt update && sudo apt upgrade -y
```

**Что делает:**
- Обновляет список доступных пакетов
- Обновляет систему до последних версий
- Займет 2-5 минут (наберитесь терпения)

---

### ШАГ 2: Установка базовых утилит

После завершения предыдущей команды, выполните:

```bash
sudo apt install -y curl wget git vim ufw htop
```

**Что делает:**
- Устанавливает полезные программы:
  - `curl` - для загрузки файлов
  - `wget` - еще один способ загрузки
  - `git` - для работы с кодом
  - `vim` - текстовый редактор
  - `ufw` - файрвол (защита сервера)
  - `htop` - мониторинг системы

**Займет:** 1-2 минуты

---

### ШАГ 3: Настройка файрвола (ВАЖНО!)

Выполните команды по очереди:

```bash
# Разрешаем SSH (важно сделать ПЕРВЫМ!)
sudo ufw allow 22/tcp
```

```bash
# Разрешаем HTTP и HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
```

```bash
# Включаем файрвол
sudo ufw enable
```

**На вопрос "Command may disrupt existing ssh connections. Proceed with operation (y|n)?"**
- Введите: `y` и нажмите `Enter`

```bash
# Проверяем статус
sudo ufw status
```

**Результат должен показать:**
```
Status: active
To                         Action      From
--                         ------      ----
22/tcp                     ALLOW       Anywhere
80/tcp                     ALLOW       Anywhere
443/tcp                    ALLOW       Anywhere
```

---

## ✅ ПРОВЕРКА

После выполнения всех команд проверьте:

1. **Вы все еще подключены к серверу?**
   - Должно быть приглашение: `root@ваш-сервер:~#`

2. **Файрвол активен?**
   - Команда `sudo ufw status` показала "Status: active"

3. **Все утилиты установлены?**
   - Попробуйте: `git --version` (должно показать версию)

---

## 📝 ЧТО ЗАПИСАТЬ

**Важные данные (сохраните их!):**
- IP-адрес сервера: `_________________`
- Пароль root: `_________________`
- Тип подключения (SSH пароль / SSH ключ): `_________________`

---

## ⚠️ ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

### Проблема: Не могу подключиться по SSH

**Решение:**
1. Проверьте правильность IP-адреса
2. Проверьте пароль (может быть чувствителен к регистру)
3. Убедитесь, что в панели Beget разрешен SSH доступ
4. Попробуйте подключиться через панель Beget (обычно есть кнопка "Консоль" или "SSH")

### Проблема: Забыл пароль

**Решение:**
1. В панели Beget найдите раздел управления паролями
2. Используйте функцию "Сбросить пароль root"

### Проблема: Команда не выполняется

**Решение:**
1. Убедитесь, что вы подключены (видите приглашение `root@...`)
2. Проверьте, что скопировали команду полностью
3. Попробуйте ввести команду вручную

---

## ⏭️ СЛЕДУЮЩИЙ ШАГ

**После выполнения всех команд напишите мне:**
- ✅ "Готово, подключился и выполнил настройку"
- Или опишите, на каком шаге возникла проблема

---

**Статус:** ⏳ Ожидаю выполнения Действия 2

**Время на выполнение:** 10-15 минут

