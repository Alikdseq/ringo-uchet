# ✅ НАСТРОЙКА SSL - ПОШАГОВАЯ ИНСТРУКЦИЯ

## 🎯 ДОМЕН
`ringoouchet.ru`

---

## 📋 ШАГ 1: ОБНОВИТЬ NGINX КОНФИГУРАЦИЮ

### 1.1 Открыть конфигурацию

```bash
sudo nano /etc/nginx/sites-available/ringo-uchet
```

### 1.2 Обновить конфигурацию

**Замените `server_name` на домен и добавьте блок для Certbot:**

**В HTTP блоке (строка 37) измените:**

```nginx
    server_name ringoouchet.ru www.ringoouchet.ru 91.229.90.72;
```

**И добавьте блок для Certbot ПЕРЕД первым блоком `location / {` (после строки с `client_max_body_size`, перед строкой 47 где `location / {`):**

```nginx
    # Для Certbot - получение SSL сертификата
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Статические файлы Flutter Web приложения
    location / {
```

**Сохраните:** `Ctrl + O`, `Enter`, `Ctrl + X`

---

## 📋 ШАГ 2: СОЗДАТЬ ДИРЕКТОРИЮ ДЛЯ CERTBOT

```bash
sudo mkdir -p /var/www/certbot
sudo chown -R www-data:www-data /var/www/certbot
sudo chmod -R 755 /var/www/certbot
```

---

## 📋 ШАГ 3: ПРОВЕРИТЬ И ПЕРЕЗАГРУЗИТЬ NGINX

```bash
sudo nginx -t
```

**Если все хорошо:**

```bash
sudo systemctl reload nginx
```

---

## 📋 ШАГ 4: ПОЛУЧИТЬ SSL СЕРТИФИКАТ (ВЕБРООТ МЕТОД)

```bash
sudo certbot certonly --webroot -w /var/www/certbot -d ringoouchet.ru -d www.ringoouchet.ru
```

**Следуйте инструкциям:**
- Введите email
- Согласитесь с условиями (A)
- Подписка (можно отказаться - N)

**Должно показать:** `Congratulations! Your certificate and chain have been saved`

---

## 📋 ШАГ 5: ДОБАВИТЬ HTTPS БЛОК В NGINX

**После получения сертификата:**

```bash
sudo nano /etc/nginx/sites-available/ringo-uchet
```

**В КОНЦЕ файла добавьте HTTPS блок (полная конфигурация будет в следующем шаге).**

---

**Выполните шаги 1-4 и сообщите результат!**

