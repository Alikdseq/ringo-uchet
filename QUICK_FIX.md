# 🚀 Быстрое исправление ошибки 502

## ⚡ Самый быстрый способ (автоматический)

Откройте **PowerShell** в папке `C:\ringo-uchet\` и выполните:

```powershell
.\fix-502.ps1
```

Скрипт автоматически выполнит все необходимые шаги.

---

## 📋 Ручной способ (пошагово)

### Где выполнять команды:
**PowerShell** или **Command Prompt** в папке `C:\ringo-uchet\`

### Команды (копировать по порядку):

```powershell
# 1. Перейти в корень проекта
cd C:\ringo-uchet

# 2. Остановить все контейнеры
docker-compose down

# 3. Пересобрать frontend (займет 5-10 минут)
docker-compose build --no-cache frontend

# 4. Запустить все сервисы
docker-compose up -d

# 5. Подождать 10 секунд для запуска
Start-Sleep -Seconds 10

# 6. Проверить статус
docker-compose ps

# 7. Проверить логи
docker-compose logs --tail=50 frontend
docker-compose logs --tail=50 nginx
```

---

## ✅ Проверка результата

### В браузере:
1. Откройте `http://localhost`
2. Нажмите `F12` → вкладка **Console**
3. **Не должно быть ошибок 502**

### Очистка кэша браузера (если ошибки остались):
1. `F12` → **Application** → **Service Workers** → **Unregister**
2. `F12` → **Application** → **Storage** → **Clear site data**
3. Обновите страницу (`Ctrl+F5`)

---

## 🔍 Если что-то не работает

### Проверить статус всех контейнеров:
```powershell
docker-compose ps
```
Все должны быть в статусе `Up`

### Посмотреть логи с ошибками:
```powershell
docker-compose logs frontend | Select-String "error" -Context 5
docker-compose logs nginx | Select-String "error" -Context 5
```

### Перезапустить конкретный сервис:
```powershell
docker-compose restart frontend
docker-compose restart nginx
```

### Полная переустановка (если ничего не помогает):
```powershell
docker-compose down -v
docker-compose build --no-cache
docker-compose up -d
```

---

## 📞 Что проверить если ошибка осталась

1. **Порты не заняты?**
   ```powershell
   netstat -ano | findstr ":80"
   netstat -ano | findstr ":3000"
   ```

2. **Контейнеры запущены?**
   ```powershell
   docker-compose ps
   ```

3. **Логи показывают ошибки?**
   ```powershell
   docker-compose logs frontend
   docker-compose logs nginx
   ```

4. **Файлы на месте?**
   ```powershell
   Test-Path frontend\Dockerfile
   Test-Path infra\nginx\default.conf
   ```

---

## 🎯 Ожидаемый результат

После выполнения команд:
- ✅ Все контейнеры в статусе `Up`
- ✅ В браузере нет ошибок 502
- ✅ Service Worker зарегистрирован
- ✅ API запросы работают

---

**Время выполнения:** 5-15 минут (зависит от скорости интернета)

